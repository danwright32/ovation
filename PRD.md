# Ovation

Status: Draft for review by Dan Wright
Date: 2026-08-27
Author: Dan Wright

Every decision below was made by Dan during an interview on 2026-08-25 to 2026-08-27. Anything not settled there is marked as proposed or listed as an open question with an owner.

---

## 1. Problem

Dan runs a one person photography business, shooting roughly 30 to 40 performances a year. The money side of that business runs on a paid subscription product whose features he almost entirely does not use, and the work of getting paid is spread across three systems that do not know about each other.

Observable symptoms:

1. Dan pays a monthly QuickBooks subscription and uses a small fraction of what it does.
2. Every shoot is entered twice by hand. Once in Downbeat, which knows the client, the venue, the dates and the times, and again in QuickBooks, which knows none of that and has to be told.
3. Getting paid is tracked as a sequence of manual reminders in OmniFocus rather than as a state anything can report on. There are separate tasks to create an estimate, convert it to an invoice, send it, check it was paid, and mark it paid.
4. Receipts are captured on a phone at the point of purchase and have no route into any system until Dan moves them by hand.
5. At tax time Dan answers an accountant's questions by assembling the answers himself.

In Dan's framing (2026-08-25): "It's largely just that I'm paying for it and I don't have to be. I don't use most of the features." And on what he wants instead: "I just want something that does exactly what I need. No more or less."

---

## 2. What this product is

Ovation is a native macOS application that holds the money side of Dan Wright Photography: what was billed, what was paid, what was spent, and the records behind all three. It has two halves.

1. **Invoicing and payment.** A booking committed in Downbeat becomes a priced draft invoice in Ovation with no typing. Dan reviews it, sends it as a PDF through Gmail, and records payments against it until it is settled.
2. **Expenses and the tax record.** Receipts arrive by email, are read on the Mac, and are filed against categories that map to the tax lines an accountant works in. At year end Ovation exports what the accountant needs.

What already exists: Downbeat, which records every booking and already publishes a versioned JSON file that Overture reads. Overture, which already holds a proven Gmail connection built on PKCE OAuth. QuickBooks, which holds 2026 to date and is being replaced.

What Ovation adds: the pricing, the invoice document, the payment record, the expense record, and the export.

---

## 3. Who it is for

1. **Dan, the design center.** Sole user. Every decision optimises for his daily use, subject to the two floors below.
2. **Dan's accountant.** Never opens the app. Receives two CSV files and must be able to answer their own questions from them. This is a floor, not a preference: an export the accountant cannot work with is a failure regardless of how good the app feels.
3. **An accounts payable clerk at a client organisation.** Never opens the app. Receives a PDF and must be able to pay from it without emailing Dan first. Also a floor.

Not for: anyone else. There are no other users, no roles and no sharing. Ovation runs on Daniels MacBook Pro 2 and nowhere else, because Downbeat's handoff is a file on that disk.

---

## 4. What success must look like

**Primary measure.** One full tax year is completed in Ovation with no fallback to QuickBooks. Concretely: tax year 2026 is filed from Ovation's export, and QuickBooks is cancelled.

**Baseline.** The QuickBooks subscription cost is TBD, see 9.1. The count of 2026 records to migrate is TBD, see 9.2. Both come from Dan; nothing here invents them.

**Ranked seconds:**

1. No shoot goes unbilled. Ovation can state a checkable fact: bookings received, invoices created, and any difference between them named.
2. Every expense Ovation holds has a receipt or is explicitly marked as having none.
3. The accountant asks no question the two CSV files cannot answer.

**Directional, not pass or fail:** time spent at tax time, and how quickly invoices are paid.

**A measure deliberately not claimed.** Ovation cannot promise every expense was captured. Dan declined statement reconciliation, so nothing independent exists to compare against. Ovation will not be able to distinguish a month in which Dan spent nothing from a month in which he stopped filing, and it will not assert that it can.

---

## 5. What Ovation must do

### Invoices

1. Every committed Downbeat booking becomes a draft invoice, and Ovation can report bookings received against invoices created, naming any difference.
1a. **The unit of billing is the engagement, not the show, and only Dan can say which shoots are one engagement** (Dan, 2026-09-06, reversing his own decision of 2026-08-27 recorded in non goal 8). His example is what settles it: a rehearsal on the Wednesday and the concert on the Saturday are one invoice, while two unrelated concerts a fortnight apart are two. Neither consecutive dates nor a shared booking pass can tell those apart, so nothing infers it. **The default stays one draft per booking, always**, and Dan combines two or more DRAFTS for the same client into a single invoice, one number, one due date, each shoot its own line. It is rare, so it is a correction rather than a step in the flow, and Ovation never proposes it (non goal 8). Three things follow. Combining is available on drafts only, and a draft carries no invoice number (47), so nothing burns a number from the sequence in 6. Nothing in the Downbeat handoff changes, because the combining happens entirely inside Ovation after the queue has been drained. And a combined booking is one of the three endings in 1b, which is where the consequence for requirement 1's reconcile is stated, so it is said once rather than in two places that can drift apart.
1b. **A booking has exactly three endings, and each is RECORDED rather than inferred** (Dan, 2026-09-06). It becomes its own invoice, which is the ordinary case. It is combined with another booking onto one invoice under 1a. Or Dan dismisses it and it is never billed, which is what usually happens to work done for nothing. Those three are what the reconcile in 1 counts, and they are the reason it can no longer expect bookings received to equal invoices created (ovation#36, L98). **A zero invoice is not a fourth ending**, it is an ordinary invoice that happens to total nothing: occasionally a corporate client's accounts payable needs the paperwork for a comped shoot, so it is drafted, numbered and sent like any other. **A zero total is therefore legitimate and no guard may refuse one.** The refusal in 3b is about a nonsense DURATION, not a small or absent amount, and a validator written to reject an invoice totalling nothing would refuse exactly the case Dan named. **Dismissing keeps the row and records the decision and its date**, so it is never a delete, and it is what finally lets Ovation tell a comped shoot from an invoice Dan forgot, which section 6 records as something it cannot do.
1c. **Dismissing is done from INSIDE the invoice, and never appears on the invoice list** (Dan, 2026-09-07, having been shown four ways of putting it on the list and rejecting all four). His reason is a rule rather than a preference, and it generalises: **space on the main list is earned by frequency, and this is rare.** Four treatments were rendered and measured, including the quietest one and the one that hid it in a right click menu on the row, and the objection held against every one: a standing offer not to bill, on a screen where nearly every draft will in fact be sent, costs a permanent share of the one list Dan looks at every day. So the list is UNCHANGED by 1b. The cost, stated because it is real: the pile of passed unsent drafts stays a mixture of invoices Dan owes and shoots he was never going to bill, and the only way to resolve one is to open it. That is accepted, because opening a rare thing is cheaper than every ordinary row paying for it.
2. An invoice can also be created from scratch, with no booking behind it, for work not booked through Downbeat.
2a. **Every invoice carries a kind, and it reaches the export as its own column** (Dan, 2026-09-06). The starting list is photography, print sale, licensing, and other. An invoice drafted from a booking is photography without being asked. An invoice created from scratch under 2 asks, because that is the whole population where the answer is not obvious. The completeness of the list is enforced by a test over the enum rather than by a default branch, the same way the Schedule C map is in 19a, so a kind added without an export mapping fails loudly instead of landing somewhere plausible (L113).
2b. **An imported invoice gets a kind meaning "not recorded", never photography.** QuickBooks rows carry no such field, so defaulting them to the commonest value would assert a fact nobody ever recorded, and it would do so in the direction that reads as complete: every row filled, nothing to chase, the gap invisible (L192, L548). It is a real value in the list, it is visible, and any figure computed per kind says how much of the money it could not place. This is why the kind is worth adding NOW rather than when it is first wanted: it cannot be recovered afterwards for invoices already sent.
2c. **Whether a print sale is taxed like Dan's time is a question for the accountant, not a decision taken here** (see 9). Requirement 5 charges one rate on everything, which is correct for photography services and may not be for a printed photograph sold in New York. If the answer is that they differ, the tax rule branches on the kind in 2a, which is the reason the kind is a property of the invoice rather than a label added at export time.
3. Pricing is $250 per hour, **exact to the QUARTER hour**, with a one hour minimum. **Corrected 2026-09-08 (ovation#127); it said "exact to one decimal" and that was wrong by construction.** Round 4 of the invoice screen rounds a shoot to the nearest quarter, which Dan chose, and a quarter is 0.25: 1.25 and 1.75 hours cannot be written to one decimal at all. Measured from the FreshBooks export, of 162 billed lines on 130 issued invoices, 94% land on a quarter hour and 78% on a half, with 1.75 on eleven lines and 2.25, 3.25, 4.25, 4.75 and 6.25 besides; two more, 2.15 and 1.15, are on neither a quarter nor a tenth. A validator written from the old wording would have refused the commonest values in his real history, and a duration stored to one decimal turns 1.75 hours into 1.8 and prices a shoot $12.50 over what it ran. `Hours` therefore stores HUNDREDTHS of an hour, which holds every value above exactly. **The number of hours is DAN'S INPUT on the invoice, not a value derived from the booking** (see 3a, corrected 2026-09-07).
3a. **CORRECTED 2026-09-07. The booking's start and end times are a booking time PLACEHOLDER and never become the real ones, so nothing may be priced from them.** What replaced the previous text is the answer to the question that text asked for.

**What Dan actually does** (his words, 2026-09-07): "performing arts events have variable times. So even if somebody thinks a shoot is two hours, it might run long and go to 2 1/2 hours and it might run short and be an hour and 45 minutes. So I don't finalize the invoice until after the shoot is over. That way, I know the actual end time of the event. I default everything to an hour, and then adjust after I'm done shooting for the evening." And, decisively: "I do not go back into downbeat and correct the shoot after the fact."

So he adjusts the duration ON THE INVOICE. The booking keeps its one hour default for ever, because nothing ever goes back to change it. Measured: 16 of 19 committed bookings carry exactly 3600 seconds (`scripts/measure-booking-export.py`), and `BookingDraft.defaultEnd()` in Downbeat is `defaultStart().addingTimeInterval(3600)`, re-derived whenever the start moves. Against that, Dan's own invoicing history shows the opposite: **133 photography lines on ISSUED invoices, 2019 to 2024, of which only 16% are exactly 1.0 hours**, with a median of 2.00 and a maximum of 6.25. Source, and it is worth naming because the numbers came out of it rather than out of anybody's memory: `Photography Assets/Documents/Freshbooks Photography Invoices 2015-2024.csv`, exported 2025-01-18, 203 lines across 171 invoices and 41 clients. Its FILENAME says 2015; its earliest issued date is 2019-01-06. Drafts are excluded, because 41 of its lines sit on invoices that were never issued and a duration nobody finalised is not evidence of what a shoot ran to.

**What the previous text got wrong, recorded because the mistake is instructive rather than embarrassing.** It said the two instants were "when he starts and stops shooting, and the gap between them is the number he bills", confirmed by Dan on 2026-08-27. That is a true statement about what Dan BILLS and a false one about what those two FIELDS hold. A confirmation of the concept was read as a confirmation about the data, and 3a itself asked for the comparison that would have caught it. Pricing from those fields would have billed one hour for every shoot Dan has ever done, on an invoice that totals correctly and reads as entirely normal on its way to a client (L161).

**Nothing is to be reconciled against anything.** Dan, asked: "If it's going to match anything, it should match Fantastical's calendar event. But I also don't need it to match anything." So no matching mechanism is to be built, and no flag recording whether a booking's end was edited, because it never is. Downbeat needs no change for this.

**What the booking DOES supply** is the client, the shoot name, the venue and the date. Not the price.

3b. A duration that is zero or negative, or implausibly long, is refused by name rather than priced. It is never rounded into a number and never fed straight into a comparison, because a value parsed from stored data that reaches arithmetic unchecked is how a nonsense figure becomes a confident invoice (L23, L50). One Downbeat booking is one show, so a drafted invoice carries one photography line plus whatever extras Dan adds. A run of three nights arrives as three bookings and drafts three invoices, which Dan may combine into one before sending (see 1a).
3c. **A drafted invoice carries NO duration, and cannot be sent until Dan supplies one** (Dan, 2026-09-07, choosing this over pre-filling one hour). Until then it has no hours and no amount. The cost is one field per invoice; what it buys is that forgetting becomes impossible rather than merely unlikely. The alternative was rejected on a measurement: 16% of his issued invoices over 2019 to 2024 were exactly 1.0 hours, so an invoice sent at a pre-filled hour would be indistinguishable from a correct one on exactly the shoots where the mistake is most plausible, while being wrong on the rest. **The rate is rising and the recent figure is the one to hold this to: 28% over 2023 and 2024, with the median falling from 2.00 hours to 1.50.** So a pre-filled hour would be wrong on 72% of recent work rather than 84%, which is a narrower margin than the full period suggests and still not close. Recorded because a requirement citing the flattering number from a six year window is overstating its own evidence, and because if that trend continues the choice here is worth re-reading. This also means an unpriced draft is a legitimate state the whole product must handle, distinct from a zero total, which 1b makes an ordinary invoice.
4. Line items pick from a fixed list of service types. A new type can be added from inside an invoice without going to settings first. Proposed starting list: Photography (hourly), Rush turnaround, Preview images. **Referral credit was in this list and was removed on 2026-09-07** by the same decision as 8: it is not a line, so it is not a service type either, and offering one would put a picker entry in front of Dan that no line can legitimately use.
4a. **A discount is not a line item. It sits below the subtotal, and the tax is charged on what is left** (Dan, 2026-09-06). Any invoice may carry one, entered either as an amount in dollars or as a percentage, chosen per invoice. A percentage means a percentage of the PRE TAX subtotal, never of the tax inclusive total, because "percent of total" reads both ways and the two produce different money. The hourly rate is never touched: Dan gives the discount on the job, not on his rate, and a reduced rate on a PDF is legible as a discount to any client who compares two invoices. **There is no hide setting.** He rarely discounts at all, so most invoices simply carry no discount line, which is what "usually not shown" turned out to mean when the two readings were put in front of him rendered. A hidden discount was costed and declined in the same breath: it cannot be hidden on an itemised invoice without the page failing to add up, since either the subtotal stops explaining the tax or it stops matching the line above it.
4b. **A discount and a referral credit are different things and stay separate**, though they net out to the same tax. A referral credit sits INSIDE the subtotal, in its own block between the lines and the subtotal (8), and is earned against a ledger. **It is not a line, corrected 2026-09-07** (see 8): the conclusion here is unchanged and the reason for it was, since the credit reaches the tax base the same way either way. A discount is below the subtotal, is a decision Dan makes on the day, and answers to nothing. Their effect on the tax base is identical, which is exactly why an implementer might merge them, and the export needs them apart to answer how much was given away in a year.
5a0. **Two figures this document cites cannot be re-derived by anybody, and that is recorded rather than left to be discovered** (ovation#121). How many clients carry a recorded TAX STATUS ("6 of 31") and how much money is HELD on a client are in neither committed source: the Downbeat export has no field for either, and the FreshBooks export is invoices rather than clients. `scripts/measure-booking-export.py` says so on every run rather than only when somebody asks, so the two do not read as reproducible alongside the figures beside them. Every other number in this document about invoices comes from `scripts/measure-invoice-history.py` and every one about bookings, clients or addresses from `scripts/measure-booking-export.py`.

5a. **A one pass setup screen clears the roster before any of these guards go live**, because measured against the live export on 2026-08-28 they would fire on most invoices from day one and a warning that common is one Dan learns to click past. Of 31 clients, only 6 carry a tax status at all, so requirement 5's warning would appear on 25 of 31. **Corrected 2026-09-07 (37a, 38a, 38c): the email half of this is almost entirely fictional.** Every client has an address; the one empty value is an empty OVERRIDE, which is the ordinary case; the shared address warns rather than refusing; and a value carrying two addresses is legitimate. What is left is ONE client whose address is not an address. The screen lists every client missing or conflicting on these fields and lets Dan settle them in one sitting. Only then do the guards start speaking, and a warning after that means something. **The pass holds what BLOCKS a send and nothing else** (Dan, 2026-09-10, ovation#40, choosing this over a third section for the shared address counted in the pass's total, and over one that sits in the pass uncounted). So it is two sections, the one address that cannot be sent to and the 25 missing tax statuses, and the shared address stays where 38c put it, on the client, as a question answered where the client is. **The cost, stated because it is real: the one screen built to be cleared once does not hold every question about the roster**, so a shared address is met only by opening that client. It was rendered both ways at the real count before it was chosen. **Corrected 2026-09-11: the address section now has nothing in it.** Dan fixed the one unsendable address in Downbeat, at source, so the live export measures zero and the pass is the 25 missing tax statuses. **A section with nothing in it is not drawn**, which is the same zero rule as every count in the sidebar card and as the roster screen's own presence in the rail, and it returns the moment a client has an address that cannot be sent to. The tax figure is the half that cannot move this way: the export carries no field for tax status at all (5a0), so nothing outside Ovation can ever clear it, which is why the pass exists whatever happens to the addresses.

5. Sales tax of 8.875% applies to the subtotal AFTER any discount in 4a, is skipped for clients recorded as exempt, and **where the client's status was never recorded the tax is charged, the fact is stated on the draft, and the invoice REFUSES TO SEND until the status is answered** (Dan, 2026-09-07, round 7 of ovation#111, choosing this over stating the warning and letting the invoice go, and over asking the question at the moment of sending). A missing status is not the same as "not exempt". **This requirement said "a visible warning" until 2026-09-07 and is corrected in place**: a guard written from that wording would have shown the warning and sent the invoice anyway, while passing a reading of the requirement, which is the behaviour Dan rejected. **It is the SECOND of two refusals on one control**: an unpriced draft (3c) is the other, and the treatment round 7 settled is that each reason is stated at its cause, the send is greyed, and a tip on the send names what it is waiting on. Both come from one list, so a send control cannot be enabled by whichever question it asked last. **The sequencing consequence is a hard prerequisite rather than prudence**: 25 of 31 clients carry no tax status, so 80% of invoices cannot be sent on the day invoicing ships, and 5a's roster pass has to clear first (ovation#40).
5b. **That warning is shown to Dan and NEVER to the client** (Dan, 2026-09-07). "On the draft" in 5 meant the draft inside Ovation, and the ambiguity was found while building the invoice PDF fixture, where the other reading would have put it on the page. The PDF the client receives says nothing about the exempt status being unrecorded: it charges the tax and shows it like any other invoice. The reason is the count. Measured against the live export, 25 of 31 clients carry no tax status, so the other reading would tell most of Dan's clients, in writing, that he is not sure whether they should have been charged, on a document their accounts department reads. **This is a constraint on the PDF renderer rather than a preference**, so it is asserted rather than remembered: a test renders an invoice for a client with no recorded status and refuses any mention of the status on the artifact, which is derived from the rendered document and never from the settings string (the same discipline 41a already requires of the payment block).

5c. **An exempt client's invoice still DRAWS a tax line, reading $0.00** (Dan, 2026-09-07). Requirement 5 skips the CHARGE for an exempt client; it does not remove the line. Dan's reason is the reader's: "I want to show tax = $0 to the receiving party so they know I'm not charging tax." An invoice that simply omits tax leaves the client unable to tell a deliberate exemption from an oversight, and the party who has to tell them apart is an accounts department that has never met him.

**This is the second case of the exception already written beside the zero rule, and the first draft of the invoice PDF got it wrong.** "A quantity of nothing is not drawn" was settled over four rounds on the Clients screen and reads, in full, that an absence meaning *nothing is waiting here* is not drawn while an absence meaning *something should be here and is not* is drawn. A tax of $0.00 on an exempt client is neither: it is a MEASURED value that happens to be zero, which is the same category as the comped invoice totalling $0.00 in 1b, and measured zeroes are always drawn. The design round had removed the line by analogy with the sidebar counts, which was the rule applied outside the domain it was written for.

So the tax line is present on every invoice. Where the client is exempt it names the reason rather than showing a rate against nothing, since "8.875%" beside "$0.00" is arithmetic the reader cannot check. The exact wording is outbound copy and gets its cold read under 41a.

6. Invoice numbers come from one continuous sequence starting at 1123. A cancelled invoice keeps its number.
7. The invoice is dated its booking's shoot date and is due 14 days later. Overridable per client and per invoice. Ovation warns at send time when the due date is already close or past.
8. A referral credit appears in **its own block between the line items and the subtotal**, not as a line. **Corrected 2026-09-07** by Dan in round 6 of the invoice screen rounds (ovation#111, ovation#126), having been shown that it contradicted this requirement as first written, which said "its own line". It is still inside the subtotal, so the arithmetic and 4b's conclusion are unaffected. One hour is earned per hour of the referred client's first booking, once only, credited when that first invoice is marked paid. Ovation keeps the balance per client and warns when a booking is flagged as spending credit the client does not have.
9. The PDF carries Dan Wright Photography's identity, the client name alone in the Bill to block, the line items, tax, total, the payment instructions and the note to customer, all from settings.
10. Sending goes through Gmail from a review screen showing the exact PDF and **every** recipient, prefilled from where the client's invoices go (37a: the override where there is a genuine one, otherwise the address of whoever booked), with a CC available. **Where that value carries several addresses (38a) the review lists them all**, because what Dan approves must include who it goes to (L64). Ovation records what it sent, to whom, and when.
10c. **The preview IS the attachment. One render, shown and sent, never two** (Dan, 2026-09-09,
during the review screen rounds: "when this is actually built and I view this page in the app, the
preview I see before sending needs to be identical to what's actually sent"). Requirement 10 said
"the exact PDF", and an implementer can satisfy a reading of that with a faithful redrawing of the
invoice on screen beside a separately generated file. That is the thing this forbids. Ovation
renders the PDF once, to bytes; the review screen displays THOSE BYTES; the send attaches the SAME
BYTES, the same object, not a second render from the same data. **Two renderers agree on the day
they are written and then drift**, and the drift is silent in the worst direction: the preview goes
on looking correct while the client receives something else, and the only person who could notice
is the one who never sees the file. It is the same shape as L58, two systems that must agree cannot
be checked against records one of them wrote, and as 41a's reason for a cold read: outbound work is
the only thing here with no reader on our side.

Three consequences, written here rather than left to be rediscovered. **The preview inherits the
page**, so it is a US Letter page at the proportions 5.9 settled and it can be scaled but never
restyled, and a review screen may not re-typeset it to fit a column. **The bytes are what gets
recorded** under 11, which keeps every version sent, so the artifact stored is the artifact
displayed and the artifact received, all one. And **it is testable rather than a principle**: the
guard is that what the screen renders and what the message carries are the same bytes, by hash, and
a test that renders twice and compares two outputs would pass while proving nothing (L70).
10b. **The message Ovation builds strips line breaks from every value that reaches a header.** Overture's message builder, which the shared package is extracted from, writes the subject, the recipient and the sender name straight into the headers with no such stripping, and encodes the subject only when it contains non ASCII. So a plain ASCII subject carrying a line break passes through and becomes an additional header, for instance a second recipient. It is harmless in Overture today, and it would not be in Ovation, whose subjects carry the client name and shoot name. The fix belongs in the shared package so both apps get it, and what a person approves must include who it goes to.

10a. Dan cannot mark an invoice Sent by hand. Sent is only ever observed, by one of two routes: Ovation sent it, or Ovation found a message carrying that invoice number in the Gmail Sent folder and derived it. The second route exists because Ovation already reads that mailbox, and because a flag written only by actions inside the product is permanently wrong for work done in the mail client instead (L162, and the same defect already shipped in Overture as `replyHandledAt`). An invoice sent from Spark stops claiming to be a draft; the invoice records which route established it.
11. A sent invoice can be edited and resent. Ovation keeps the history of what changed and the PDF of every version sent.
12. An overdue invoice can produce a drafted reminder that Dan reviews before it goes. Nothing is ever sent to a client without Dan seeing it.
13. Cancelling an invoice that carries payments asks what happened to the money and records a refund with its own date. Under the accrual basis confirmed in 24, cancelling matters more than it did: the invoice itself was income in the year it was issued, so cancelling one issued in a PRIOR tax year removes income from a return already filed. Ovation must refuse to silently cancel such an invoice, and instead say which filed year it belongs to, so Dan can raise it with his accountant rather than discovering the discrepancy later.

### Payments

14. **A payment is its own record, not a child of an invoice** (Dan, 2026-09-06, replacing the earlier shape in which an invoice held its payments). It carries a date, an amount and a method, and it is ALLOCATED across one or more invoices. One check settling two invoices is one record for the amount actually written, split between them. Paid in full is still calculated and never typed, but it is calculated from what is allocated to that invoice rather than from a list of payments hanging off it.
14a. **Money received and not yet allocated sits visibly on the client, and that is a real state rather than an error.** It is where an overpayment goes, and it is where a deposit taken before the shoot goes, which is the shape ovation#96 was filed for. Ovation shows it as money it is holding on that client's behalf, and allocating it later is an ordinary action, not a correction. **It belongs to the CLIENT and not to any invoice** (Dan, 2026-09-07). **That it is surfaced under Clients and NEVER on the invoice list was true when this was written and is not any more**, corrected here rather than annotated: 46b puts a held money figure in the rail on every screen the app draws, 46c keeps it there beside a per client figure, and 46d puts a GROUP of invoices on the list where a client's held money could settle more than one of them. The part that survives is that the money belongs to the client: what the list carries is invoices and a figure about them, never a client's balance as a row. **How it appears there was settled over rounds A to F of ovation#98 on 2026-09-10** and is recorded in 14f, 14l and 14n. What follows was true until then and is kept because what a decision replaced is part of it: exactly how it appears there is deliberately NOT decided yet, and was not guessed: the Clients screen has not been designed, and settling one element of a screen that does not exist decides the screen by accident. The question comes back when Clients is designed, which is what ovation#98 exists to make sure happens. **Settled 2026-09-10 over five rounds, and the answer splits the question in two: it is SHOWN under Clients and APPLIED on the invoice** (14f to 14k). It does earn a line in the sidebar card, which took the card from four counts to five, settled 2026-09-07.
14b. **The allocations of one payment may never exceed its amount**, and that is enforced where it is stored rather than by whichever screen happens to be allocating. Two allocations made at once must not both fit against the same remaining balance, so it needs a constraint or a lock and not careful ordering in application code. A sum that quietly exceeds the money actually received produces invoices reading as paid out of money nobody sent.
14c. **Unallocated money and referral credit are different things and stay apart**, and 14f is how the Clients screen draws that. For the same reason 4b keeps a discount and a referral credit apart. Referral credit is earned against a ledger and is spent INSIDE an invoice, in its own block above the subtotal (8). Unallocated money was actually received and is owed back if it is never used. They will look alike on a client screen, both being a balance, and merging them would let credit that was never paid settle an invoice.
14d. **Cancelling an invoice that carries allocations releases them rather than deleting them** (13). The money still exists and it still arrived, so it returns to unallocated on the client, and Dan then decides between allocating it elsewhere and refunding it with its own date. The prior year refusal in 13 is unchanged.

14e. **A refund NAMES the payment it came out of, and money held on a client is netted against refunds** (ovation#174, corrected 2026-09-09). 14d's release is right and is not the whole of it: a release and a refund are different events and neither can be inferred from the other, because keeping the money on the client releases exactly as much and the money really is still there. Until this was corrected the refund recorded no payment at all, so nothing reduced what the payment still held and both `Payment.unallocated` and the Clients screen's held money rose by the full refunded amount and stayed there. Section 6 makes a refund the ORDINARY ending for a deposit, so that was every refund, permanently, and it could not be netted afterwards because the row named nothing to net against. **A refund spanning two payments records one row per payment**, taken in allocation order with the oldest first, because an invoice settled by two payments is ordinary and a single row for the total would name a payment the money did not all come out of. **A PARTIAL refund is the case that decides whether the two figures are netted rather than both driven to zero together**, and it leaves exactly the rest held.
14f. **On the Clients screen the two balances keep separate boxes and are told apart by their FIGURES** (Dan, 2026-09-10, round A of ovation#98, settling 14c's "they will look alike"). Money held is dollars and is set in the mono tabular face; referral credit is hours and is set in the body face at the same size and weight. **The rule is scoped to those boxes and no further**, and that narrowness is the decision rather than an accident: "mono is the money face" was the reason first offered for it and is not true of this product, since the sidebar counts, the invoice's date panel and its event history are all mono and none of them is money. Measured across all five design files before it was written down, and Dan chose the narrow rule over sweeping four settled screens to make the broad one true.

14g. **Applying held money to an invoice lives ON THE INVOICE, and the Clients screen carries no control for it** (Dan, 2026-09-10, round B of ovation#109). The money is shown where it belongs, to the client, and moved where it is spent. Put to him with the frequency: across 2015 to 2024, 6 of 171 invoices were paid before they were issued, median 11 days early, which is a floor rather than a total because the FreshBooks export carries no payment amounts and no balances, so an overpayment leaves no trace in it. A rare action earns its place or it does not get one. This makes "nothing on the Clients screen is an action" a decision rather than the gap the design record recorded it as.

14h. **Ovation applies held money ITSELF, and says so, whenever the client has exactly ONE OPEN invoice, and a PAID invoice is not one** (Dan, 2026-09-10, round C of ovation#109). There is no offer to accept: a deposit exists in order to be used. Kept against an offer under the Total, an offer in the foot beside the main action, and an offer inside the history pane, which is closed by default and so would not exist until somebody opened it. **A paid invoice gets neither the applied line nor the offer**, and both halves are stated because refusing only the arithmetic leaves an invoice reading Paid in full with a control asking whether to put more money on it. The design file did exactly that for the length of one commit, saying Paid in full in its foot while its totals said 408.28 of held money was applied and 0.00 outstanding, which in the built product is the same money counted twice. No reading of the source showed it; driving the state switch did.

14i. **The way back out is called `Remove` and sits on the held money line itself** (Dan, 2026-09-10, rounds C1 and C2 of ovation#109). `Remove` is already the word this screen uses for taking a discount off, so it is one vocabulary rather than a second; `Undo` is refused because the Edit menu has one and two things named Undo on one screen mean different things. On its own line beneath Outstanding it named nothing, which is what Dan rejected. It fits the totals row's 208px label column, which was measured rather than assumed: the discount's controls do NOT fit that column and two attempts to widen the row put the discount's figure off the one shared right edge.

14j. **With MORE THAN ONE open invoice for that client, nothing is applied to any of them**, and each says why and offers `Use it here` (Dan, 2026-09-10, round C3 of ovation#109). 14b makes exactly one invoice able to carry it, so something must choose, and a chooser nobody can see is worse than a question. Kept against naming which invoice took it, and against saying nothing.

14k. **An applied payment sits BELOW the total and never inside the subtotal**, so it can never reach the tax base. A referral credit is a negative line inside the subtotal because it changes what is being charged (8); this does not. **Where the invoice is smaller than the balance, the invoice states what is left held**, since 14e leaves exactly the rest and two screens showing 500.00 held against 440.94 applied is the product asking Dan to do arithmetic it has already done. `scripts/check-invoice-screen-draws.sh` presses `Remove` and compares the subtotal, the tax and the Total before and after, and its suite plants a version that folds the payment into the total.

14l. **Where held money came from is NAMED on the Clients screen, and listed when a balance is more than one arrival** (Dan, 2026-09-10, round E of ovation#98, answering ovation#96). A deposit reads `Deposit received 3 Sep`, an overpayment reads `Overpaid on invoice 1126`. **A single arrival is never broken down**, because one row restating the figure standing above it is the same number twice. This is as far as a deposit and an overpayment are told apart: they behave identically once received, and nothing else in the product distinguishes them.

14m. **Where an Outstanding line is drawn it carries the weight, and Total drops to an ordinary line** (Dan, 2026-09-10, ovation#189). An invoice carrying held money drew two heavy figures two lines apart and nothing said which one the eye should land on. They answer different questions and are read at different moments: Total is what the invoice is for and is the figure the client sees on the PDF, and Outstanding is what is still owed and is what the chase and the To chase count are timed from, so the rule is that the heavy figure is always what is still OWED. **Where there is no Outstanding line, which is almost every invoice, Total is what is owed and is unchanged.** That half is stated because a rule quietening Total everywhere would be judged on the rare invoice and would ship on the common one, and it is why the treatment is scoped to a totals block that actually drew an Outstanding line rather than to the Total row itself. Rejected: both heavy, which is what it drew, and Total carrying with Outstanding beneath it. `scripts/check-invoice-screen-draws.sh` measures both figures in a rendering and asserts the relationship in both states, and its suite plants a version aimed at a class nothing carries and a version with the scope dropped.
14n. **On the Clients screen the held money figure is drawn on every name row that has one EXCEPT the row whose box is open beside it** (Dan, 2026-09-10, ovation#98). Money held is named three times on that screen: the rail's all client total, the row, and the selected client's box. 46c fixed the rail as the same on every screen, so the row was what was left, and the row keeps its figure everywhere except where the box is already stating it. **Rejected: taking the figure off every row**, which costs the only way to see who is holding money without clicking through 31 names, and **rejected: leaving all three**, which states one client's money twice at once. Measured: 3 of the 31 clients hold money, so the figure is drawn on three rows and moves as the selection moves. **The consequence for code is that the row depends on the SELECTION**, which is what makes this a requirement rather than a rendering detail: a version written only where the row is built draws correctly on load and never again, reads as correct in the source, and is one of the defects `scripts/test-clients-screen-draws.sh` plants. `scripts/check-clients-screen-draws.sh` presses the rows and asserts the figure moves.

15. Methods are Check, Zelle, Venmo and PayPal. A payment by check gains a Cleared step; other methods do not. **Cleared belongs to the payment, never to an allocation**: one check clears once even where it settled two invoices, so an invoice can never be cleared on its own. The sidebar's To confirm count reads through to the payment (46a).

### Expenses

16. Receipts arrive through a Gmail folder. An email with attachments files those; an email with none has its body rendered and captured as the receipt. Several real attachments become several unfiled receipts.
16a. Signature images and tracking pixels are ignored. Because that is a filter matching on shape, it must be tested against the receipts it has to PRESERVE and not only the junk it has to catch (L104): a small receipt image looks very like a logo, and an over matching filter reads exactly like the feature working.
17. Ovation saves the expense record before it touches the email, then moves the logged message to a Logged subfolder. Nothing is deleted.
17a. **Moving the message to Logged is required, not a nice to have** (Dan, 2026-08-27). Seeing the Receipts folder empty is how he knows a receipt was filed, and a list inside Ovation does not give him that. So if Google refuses the permission this is a blocker to solve rather than a fallback to accept. Evidence that it will not come to that: Overture already holds `gmail.readonly` and `gmail.send` on a desktop app with long lived refresh tokens, and both sit in the same restricted tier as `gmail.modify`, so this is one more scope on a path that already works rather than a new verification hurdle. Worth confirming once, at the moment the scope is first requested, rather than assuming.
17b. The confinement in 17a does not loosen anything in 6.35 or the mailbox rules: Ovation may only ever act on a message it has re-fetched and confirmed still carries the Receipts label, it may never delete or archive, and every move is recorded so it can be undone.
18. Amount, vendor and date are read on the Mac, and a field is filled only where the reading is unambiguous. Where it is doubtful the field is left empty with the competing readings shown. Nothing is ever guessed.

18a. **Corrected 2026-08-27. There is only one reader, and the ambiguity signal comes from inside it.** This requirement previously specified two independent readers that had to agree. That is not achievable: verified against the macOS 26.5 SDK, the on device Foundation Models framework takes no image input. It can only re-read text that Vision has already extracted, so its agreement corroborates nothing about a number Vision misread, and treating it as a second opinion would manufacture false confidence in exactly the field that matters most. Instead Vision's own alternative candidate readings and its per observation confidence are the independent signal: where a close second candidate exists, for instance $88.20 against $38.20, the field is left empty and both candidates are shown. The behaviour Dan approved is unchanged; what produces it is now something that exists. A language model may still be used to LABEL text (deciding which line is the vendor), never to confirm a digit.

18b. **Ambiguity is expected, so it is counted rather than only handled.** Ovation keeps a running record of how often a field is left empty for doubt, per field, and Dan can see it. Leaving a field empty is correct, but it is also the path a systemic failure takes: a reader quietly degrading looks exactly like ordinary noise, one receipt at a time, and stays invisible for as long as it lasts (L77). Downbeat keeps a questionnaire agreement log for this reason and it is what turned a suspected data problem into a located parser fault. The log records counts and field names only, never the contents of a receipt.

18c. **The reader's identity is recorded with every measurement.** Vision is supplied by the operating system and changes under a system update with no build and no code change, so a number that decides whether the expense half is viable must be pinned to the request revision and OS version it was measured on (L25). Otherwise an update can lower the accuracy silently, in the reassuring direction: more fields are left empty, which reads as caution rather than as regression.

18d. **This is measured before the rest of the expense half is built.** The reader is run over a set of Dan's real receipts, photographed and digital, and the rate at which each field is filled confidently is recorded. Its accuracy on receipts is not known today, and the precedent cited for the original design was measured on typed questionnaires, not on a till receipt photographed at an angle. If fields are rarely filled, filing an expense is typing, which is the single thing most likely to make Dan stop using this half of the product. That has to be known in the first week rather than in January. **The pass marks, agreed with Dan on 2026-08-27 and recorded here before the measurement runs, so a disappointing number cannot be rationalised afterwards: amount at least 90%, date at least 90%, vendor at least 80%.** Photographed receipts and digital ones are measured separately, because they are different problems and a combined average would hide the worse half. Failing the bar means the design is reconsidered, not shipped and hoped for.
19. Every expense carries a category from Dan's own list, and every category maps to a Schedule C line. Proposed starting list, to be corrected: Gear to Supplies or Assets, Software to Office expense, Travel to Travel, Insurance to Insurance, Contract labour to Contract labor, Professional fees to Legal and professional, Marketing to Advertising, Meals to Meals.
19a. The completeness of that map is enforced by a test, not by a default branch, so a category added without a mapping fails loudly rather than landing somewhere plausible in the export (L113).
20. A single purchase above a configurable threshold is flagged as a likely asset rather than an expense, visibly and overridably. The threshold is a suggestion, not a determination.
21. An expense with no receipt is allowed and marked as such, and the mark reaches the CSV.
22. A likely duplicate, same vendor and amount and date, is flagged rather than blocked or merged.
22a. **Re-filing the same receipt twice is prevented by a key Ovation can recompute, never by an identifier Gmail mints** (L186, L127). The record that stops an attachment being filed again is keyed on the message id plus the receipt file's own SHA-256, plus the part index to separate two identical attachments in one message. Gmail's attachment id is stored for fetching but is never the key, because its stability across separate fetches of the same message is not guaranteed and a key that changes makes the record unfindable, which files a duplicate expense into a seven year tax record. Proved before it is trusted: fetch the same message twice and assert the key is identical, then assert the second run finds the record.
22b. **Every state an expense can be in is reachable from a surface, and a property test proves each lands in exactly one** (L45). The states multiply quickly: waiting in the unfiled queue, filed, no receipt, duplicate flagged, asset flagged, imported from QuickBooks with no image, read confidently, left empty for doubt, and a per attachment failure that has no expense row at all. That last one is the dangerous case, because a record with no row and no screen exists only in the data. Filtered views are the only way Dan reaches these, so a state matching no view is invisible rather than merely awkward.

### Export

23. A year end export produces income.csv and expenses.csv for any date range, defaulting to the calendar year.
24. **Income is on an accrual basis: one row per invoice issued, dated by the invoice date, whether or not it has been paid.** Dan confirmed this on 2026-08-27, having first answered the opposite before the term was explained, and reaffirmed it after being told plainly that it is a fact about his filed returns rather than a preference. Three consequences follow and each is a change from what this document said earlier. An unpaid invoice IS income for the year it was issued, so the export cannot exclude it. An invoice issued in December and paid the following January belongs entirely to the December year. And payment dates, while still recorded and still exported, no longer decide which tax year anything falls in.
24b. **Proposed, 2026-08-28, not yet confirmed by Dan: a draft is not income.** Accrual counts an invoice when it is issued, and under 10a Sent is only ever observed, so the export selects on Sent established (by Ovation's own send or the derived Gmail match) and never on creation. Two consequences the plan builds to and this document should state: a draft whose shoot has passed and was never sent is counted and named in the export's manifest rather than silently left out, and the derived Sent match in 10a is on the tax path, so an invoice sent from Spark that the match misses is missing income (see 9.14). One question this raises is recorded under 9.3 for the accountant: whether the invoice date or the send date decides the year. A second, how a refund is reported when its money moves in a different calendar year, was **settled by Dan on 2026-09-06** and is written up in section 6: the shoot date decides, refunds included.
24a. Because unpaid invoices are income, the export carries the payment state on every row, so an invoice that was billed and never collected can be identified. Writing one off as a bad debt is a decision for Dan and his accountant, not something Ovation does, but the export must make such an invoice findable rather than silently indistinguishable from a paid one.
25. income.csv carries sales tax as its own column so it can be totalled by period for the accountant's sales tax filings.
25a. income.csv also carries the kind from 2a as its own column, so photography, print sales and licensing can be totalled separately, and so the rows whose kind was never recorded (2b) are countable rather than silently mixed in.
26. Any single receipt can be exported on its own in one action.
26a. **A real export is put in front of Dan's accountant before the export milestone can close** (L246). Not a rehearsal against Ovation's own data, which only ever proves the file generates and totals against itself. Success measure 3 is that the accountant asks no question the two CSVs cannot answer, and the only way to measure that is to send them one. A partial year export, once the QuickBooks import has run, with their reply recorded and dated. If they cannot be reached, the milestone reports that it could not measure rather than closing.
26b. Requirement 24's accrual basis rests on Dan's own recall rather than his accountant's confirmation, so the same conversation settles both. See 9.3.

23a. **A year end export is run from a control Dan can press, named `Run the year end export`, in the File menu** (ovation#162). Requirement 23 said what an export produces and nothing said how one happens, so the runner shipped and was called by nothing: no menu item, no launch step, no schedule and no command. The staleness notice in 5.30 therefore named a remedy that could not be reached, and the only way to clear it was to make an export happen, which nothing could do. **The notice quotes the control's name rather than spelling it again**, so a rename cannot send Dan to a menu item that is not there. Four things follow, each because the alternative was seen to be wrong. **It exports the calendar year the clock is in**, taken from the one business calendar rather than the machine's locale, so a press in the first hours of January cannot export the wrong year. **A second press while one is running is refused**, because two exports over one folder write the same three files over each other and the second would verify against the first's partial write. **The control is never hidden, only disabled with its reason said out loud**, since a control that is not there cannot be asked why. And **a run in flight counts seconds on screen**: an action that does not return at once has to let the reader tell that it started, that it is still going, and that it ended, and a spinner that looks the same whether the work is progressing, hung or dead says none of those. The outcome reaches Dan through the one launch presenter every other condition uses, with its own kind per outcome, so a refusal, a failure and a run that wrote its files but could not be recorded never answer for one another.

### Migration and data

27. QuickBooks CSV exports import into Ovation. The import reports rows read, rows written, rows refused with the reason for each, and the totals, so they can be checked against QuickBooks before the import counts as done. A row it cannot parse is named and refused, never skipped quietly.
28. Imported 2026 expenses carry no receipt image and are marked as such.
29. Ovation writes dated, self contained backups to a folder Dan chooses, keeps a rolling set, verifies each is readable after writing, and offers Restore in the app. A backup that cannot be read back is reported as a failure, never as silence.
29b. **A backup contains the store, its write ahead log, the documents directory addressed by hash, the ledgers, the Problems store and the export run records. It EXCLUDES the credential store, and a guard asserts no backup archive contains the token file** (L19). The reason is written here rather than left to an implementer: "self contained" plus "a folder Dan chooses" plus a directory that happens to hold the Gmail refresh token is a route for a long lived credential granting send and modify on Dan's real mailbox to be copied, unencrypted, into a folder that on this Mac may be synced to a Synology. If Dan later wants a restore that does not require re authorising Gmail, that is its own decision with its own sign off, not something that arrives as a side effect of being thorough.
29a. A rolling set is a capped store, so a cheap writer evicts real records. The test suite must be structurally unable to write to the real backup folder, the real receipts folder or the real handoff queue (L2, L191). Downbeat lost a measurement this exact way: 198 of the 200 lines in its agreement log were test writes.
30. Ovation deletes nothing automatically, ever.
31. Every business date is computed in America/New_York through one shared helper, never the host clock's default (L39). Downbeat already pins that zone for the dates it exports. This decides which tax year a 31 December payment falls in, so it is tested at the month, year and midnight boundaries with a pinned clock.

### How it behaves

32. Ovation checks both queues on launch and every few minutes while running, and shows a menu bar item with what is waiting. Nothing runs while the app is closed, so every time based state, overdue included, is only as current as the last launch (L51). Ovation says when it last looked rather than implying it is always watching.
33. Every time taking action, the import, the receipt reading, the send, shows working, still alive, and failed as visibly different states. No bare indefinite spinner.
34. A malformed handoff file, or one from a Downbeat version Ovation does not understand, is refused loudly and named. It is never read as an empty file.
35. **Finding nothing is its own outcome, never success** (L98, L100). A handoff queue directory that does not exist is different from one that is empty. A Gmail receipts folder that is missing, renamed, or has "show in IMAP" switched off returns zero messages, which is otherwise indistinguishable from a week in which Dan filed nothing. Each of those is reported as what it is, and Ovation refuses to conclude anything from a zero it did not earn.
36. **The handoff queue has a redo path** (L512). Ovation keeps its own durable record of every booking identifier it has consumed, independent of the queue files it deletes, so a booking whose invoice is later lost can be recovered after Downbeat's copy has been swept.
37. A second running copy of Ovation refuses and names the copy it stood aside for, as Downbeat already does.
37a. **A client has TWO addresses and the second is an OVERRIDE, whose absence is the ordinary case** (Dan, 2026-09-07, correcting this document and the domain model). `email` is the address of whoever booked the work, and that is where an invoice goes. `contractEmail` is where it goes INSTEAD, for the handful of clients who want the invoice sent somewhere other than to the person who booked, a treasurer for the money and a director for the photographs. In his words: "not having a contract email is the default. Most clients don't have this because the person that hired me is the person I email the contract/invoices to."
37b. **Downbeat already exports both**, `email` and `contractEmail` (`mac/Overture/Domain/DownbeatExport.swift:11-12`), so this is inherited rather than invented. Ovation's first domain model dropped `email` and kept only the override, which inverted the meaning of every check written against it.
37c. **Measured against the custody export of 2026-09-05 by `scripts/measure-booking-export.py`, counts only: all 31 clients have a main address, 1 carries an empty override, and the other 30 carry an override that is an EXACT COPY of the main address. Not one client has a genuine override today.** Two consequences are requirements rather than implementation notes. An override equal to the main address is NOT an override, or thirty clients acquire a second address on every screen that shows one and thirty findings about nothing (L605). And a missing override is never a roster gap, which is what 5a and ovation#40 were built on.

38. **An address is validated against what the send path can actually consume, not against a list of things it must not be** (L150, L257). This applies to BOTH addresses in 37a, because a bad value in either reaches a send. A value is one or more addresses separated by commas or semicolons, and every part must be one `@` with no whitespace. Anything else is refused by name. Ovation never falls back to the other address, and **never sends to the parts of a broken value that happen to parse**, because that delivers an invoice to a subset nobody chose.
38a. **CORRECTED 2026-09-07 by Dan, overruling this requirement's original form.** It had refused a value carrying two comma separated addresses, on the grounds that it "puts a recipient Dan never chose onto an invoice he reviewed and approved". He did choose them: **"I should be allowed to do two addresses in one field. There's nothing stopping me from invoicing 2 emails at the same company at the same time for the same event."** So several addresses in one field is a VALUE meaning all of them, not a fault. Measured on the 2026-09-05 export by `scripts/measure-booking-export.py`: 1 of 31 clients has one, on purpose. The protection the old rule was reaching for is real and belongs in requirement 10: **the review screen shows EVERY recipient**, which is what makes what Dan approves include who it goes to (L64). What remains refused is a value that is not an address at all, including a list where any one part is not an address. Measured on the 2026-09-05 export: **1 of 31**, which is 22 characters containing a space and no `@`, human text sitting in an address field. **Corrected 2026-09-11: that value was fixed in Downbeat, at source, and the live export now measures ZERO.** Dan was shown the value and chose to correct it where client records are kept, which is why Ovation has no address editor and needs none. The refusal stays exactly as written: a client typed into Ovation by hand can still carry a value that is not an address, so zero is a fact about today's data rather than about the rule, and a rule removed because its population is currently empty is one nothing brings back. Re-derive the figure with `scripts/measure-booking-export.py` against the CURRENT export rather than reading it here (L175).
38b. **The bad value is in the MAIN address, not only in the override**, because the override is a copy of it, and it is the SAME client in both fields (verified rather than inferred). So the fix is one edit rather than two, and the roster pass has **one** address to settle rather than the four or five problems ovation#40 described.
38c. **Two clients sharing one address WARNS and never refuses, and the warning can be dismissed as correct** (Dan, 2026-09-07: "it should give me a warning but I should be allowed to dismiss it as correct"). Measured: 2 of 31 share one address. It is a question rather than a fault, so it is not one of the contact problems in 38, and the acknowledgement is recorded **against the address it was given for**, so changing that address asks again. It is consulted by every rule that raises the question, not only by the screen whose control recorded it (L330).
39. Search across invoices, clients and expenses, with filters on the invoice list.
40. **Deleting enumerates every derived resource, and the confirmation is derived from the real state rather than asserted** (L38, L180, L97). By the time invoicing ships, an invoice carries payments, refunds, the PDF of every version sent, a referral ledger entry whose balance is a sum, a consumed booking record and an import batch id; an expense carries a receipt file on disk referenced by hash, a consumed message record and per attachment intake records; a client carries all of the above. Deleting all but one of a set of linked things is the recurring defect here, and it silently corrupts both a total and an idempotency record whose whole purpose is durability.
40a. So: the confirmation names what will actually go ("this invoice carries 2 payments and 1 referral credit"), never a fixed sentence that reads identically whether it is taking one row or ten. Where a dependent exists in a ledger that must stay append only, the delete is refused outright rather than confirmed. Where deletion is allowed, the removed record stays reachable with its way back attached, because an undo that requires Dan to retype the vendor and re-supply the receipt image is not an undo: the delete destroyed the only screen showing them.
41. Accessibility is built into each control, not added afterwards.
41a. **Every outbound sentence gets a cold read, rendered, in the state that produces it, before invoicing ships** (L21, L129). This covers the payment block on the PDF, the note to customer, the covering note on a send, and the reminder body. It is called out because outbound copy is the only writing in the product with no reader: everything else Ovation says is said to Dan, who will notice, while these sentences go to strangers under his name. Overture paid for exactly this omission, where a closing note told people who had never replied that it had been good to be in touch. A check that the rendered PDF's payment block is not truncated is derived from the rendered artifact, never from the settings string it came from, since the current QuickBooks invoice truncates it mid word.
41b. **Ovation is single window on purpose, and asserts it** (L238). A condition raised once must be presented once, and a flag on shared application state is presented once per surface bound to it rather than once, so a menu bar item or a deep link that opens a second window would put up a second copy of the same notice and dismissing one would leave the other. The single running copy guard in 37 cannot see this, because a second window is the same process.
42. Ovation checks that its own stored totals agree with the parts they are made of, rather than trusting a number because it was written.
42a. **A uniqueness constraint must never be what prevents a duplicate, because on this Mac it does not refuse one, it destroys the original.** Measured directly on 2026-08-28, Swift 6.3.3 on macOS 26.5.1: a model with a unique attribute was given a second record carrying the same value, the save did NOT throw, and the fetch returned one row holding the SECOND value. The first was gone with no error anywhere. So every place that would otherwise lean on uniqueness (an invoice number, a booking consumed twice, a referral credit earned once) must instead check for the existing record and refuse in Ovation's own code before writing. The database is not a guard here; it is a silent overwrite. A test plants a duplicate and asserts the refusal, and is seen to fail before it is trusted.

42c. **Re-measured 2026-09-07 while building the domain model, and the mechanism is now named: it is the unique ATTRIBUTE that destroys the row, and Ovation therefore declares none at all, not even on the surrogate id.** 42a recorded what happened without isolating what caused it, and the plan turned that into "unique on the surrogate `id: UUID` only", which reads as the safe residue of the rule and is the opposite. `SwiftDataBehaviourTests` now holds both halves against each other, in an in memory store and a file backed one: with the attribute a colliding id leaves ONE row holding the second value, and with it removed the same collision leaves TWO rows and the original is still there. Neither outcome is good, and that is the point: the attribute does not prevent the duplicate, it chooses which failure the duplicate becomes. A duplicate is visible to the export reconciliation in 63 and can be repaired; a destroyed row cannot (L5). Ovation takes the visible failure. Every lookup that requires exactly one row therefore refuses on more than one rather than taking the first (L521), and uniqueness stays what 42a says it is: something Ovation's own code checks before writing, never something the database is asked to hold.
42b. **Receipt images and sent invoice PDFs are stored as files Ovation manages, addressed by hash, never as attachments the database stores for us.** Measured: the convenient option writes the bytes into a hidden folder beside the database rather than inside it, and Downbeat's backup copies only the database file and then verifies success by checking it opens and holds the right tables. Cloning that pattern gives seven years of backups containing zero receipts and zero PDFs, with every verification passing. Requirement 29 already says verification enumerates every referenced document and fails when one is missing; this is the storage decision that makes that possible rather than theoretical. The referral balance in particular has one declared home, the client who earned it, written by exactly one place from both the earning and the spending side (L83).

43. **Ovation is LIGHT MODE ONLY, deliberately** (Dan, 2026-09-06). It does not follow the system appearance. This is a decision with a stated cost, recorded so that the absence of a dark half in the design tokens is never read as an oversight and quietly corrected: **Dan's Mac is set to dark**, checked on the day this was decided, so Ovation will be a bright window among dark ones in the evenings, which is when receipts get filed and invoices get sent after a shoot. He accepted that in exchange for not carrying a second designed palette and a second set of contrast guards through every screen. **Both siblings do the opposite**, defining a light and a dark value for every token, so a future reader coming from Downbeat or Overture will find this surprising; that is why it is written here rather than left implicit. Reversing it is a design round on the dark half plus a second token set, not a token swap, because the sidebar in 44 works by being darker than the page and would have to invert.

44. **The accent is espresso, `#3B2B21`, and it lives in the sidebar** (Dan, 2026-09-06, after two rounds over eighteen candidates and a warm to cool ramp). The sidebar is a full height panel in that colour running to the top of the window with the traffic lights on it, which is the macOS pattern whenever a sidebar carries colour; the title bar exists only over the content. The list itself stays on pale paper and takes the colour only on the row that is selected. Colour has exactly one meaning in this app and it is not decoration, so nothing else is ever coloured.

44a. **The rail ships WITH the first screen, and a destination that is not built yet is present and says so** (Dan, 2026-09-11, ovation#40, in a round that drew all three at the real count). 5a's roster pass is the first product screen Ovation draws in SwiftUI, so the question of how much of the app surrounds it had to be answered before it was built rather than settled by whatever was easiest. He took the rail exactly as 44 describes it, carrying `Invoices`, `Expenses` and `Clients` as entries marked `not built yet`, with `Settle the roster` selected. **The cost is stated because it is real: three of the four entries lead nowhere for months.** What it buys is that the two rules 5a already settled have somewhere to be true: the roster is in the rail only while something blocks a send, and it stays while you are standing on it, and neither is expressible in a window with no rail. **Rejected: the window holding the pass alone.** It is the smallest build, and rendering it surfaced a consequence nobody had written down, which is that the rail is also where the launch notices and the backup status of 37 and 59 live, so a screen that takes the whole window takes their surface with it. **Rejected: the rail with the Clients screen finished beside it**, which ships the other half of the same design file but draws money held and referral credit before either has data behind it. **The placeholder treatment was deliberately NOT settled by a round**: it was drawn plain so that round stayed about whether the rail is there at all, and Dan chose to keep it as drawn rather than run a second round, on the stated grounds that it is four words in one place and cheap to revisit.

45. **No red anywhere in the invoice or expense surfaces, on principle** (Dan, 2026-09-06: red "feels like something is wrong"). An overdue invoice is not an error. Nothing failed, the money has not arrived, and lateness is shown as an age in tabular figures, a duration rather than an alarm. **Red belongs to the Problems store alone**, whose kinds are all genuine faults in the app itself (a foreign database, a failed backup, a stale export, an unwritable journal). If the invoice list borrows that vocabulary then the one surface that should alarm Dan has nothing left to say with. A guard asserts that no colour in the warning family reaches the list surfaces.

46. **The invoice list carries no group headings and no per row labels** (Dan, 2026-09-06). The eight groups of section 6 still decide the ORDER, and the property test on ovation#49 still applies unchanged, but they are not DRAWN. The reason is redundancy: a heading reading "Send it today" sat above a chip reading SEND which sat on a row whose action word read Send, so one fact was labelled three times, and with only one to three invoices per group nearly every other line on screen was a heading. What the row already carries covers every case the headings spelled out: draft or invoice number, the shoot date, the days late, and the exact action. **Anyone implementing ovation#49 should build the grouping and not the headings.**

46a. **The sidebar card is a ROLLUP of those groups, and the mapping is part of the requirement rather than an implementation detail.** Its five lines are not the eight groups renamed; two of them each cover two groups, and there were four until 46d added `To place` on 2026-09-10. **To send** is group 1 (draft, shoot is today) plus group 3 (draft, shoot has passed or has no date). **To chase** is group 2 (overdue). **To confirm** is group 4 (paid by check, not cleared) plus the third member of the sent triple from ovation#45 (could not determine whether it was sent). **To place** is the group 46d put at the top of the list, the invoices of a client whose held money could settle more than one of them, and it is the one line whose group is not a status: an invoice in it is also overdue, or also a draft, and it is drawn ONCE, in this group, which is why the group takes its rows out of the others rather than repeating them. What that does to ovation#49's rule that every state lands in exactly one group is that the rule now has to be read over a list where one group cuts across the rest, and that is stated here rather than left for whoever writes the property test. **Receipts to file** is the expense queue and comes from the other half of the product entirely. Without this written down the card cannot be reproduced, and worse, whoever builds it will derive the numbers separately from the list, which is precisely the drift the compute once and read twice rule on ovation#49 exists to prevent: **that rule is unenforceable without this mapping beside it.** Acting on a card line takes you to the groups it rolls up, or the first time 4 on the left is compared with 1 on the right it reads as a defect. **Every line in it counts INVOICES**, which is what makes them comparable with each other; the one line that ever counted something else is 46b, and it is no longer one of them.

46b. **Money held on clients is drawn UNDER the card and is not one of its counts** (Dan, 2026-09-10, two rounds on ovation#187). It was added as a fifth line on 2026-09-07 and three things came apart from it. It counted CLIENTS while the other four count invoices, with nothing on screen saying so, so `3` meant either. 14h then made Ovation place held money by itself whenever the client has one open invoice, so the number stopped marking work waiting and started marking money sitting, which the product now deals with on its own. And the card's promise is that a number in it means this many things need you, which that number no longer did. So it is a FIGURE, in dollars, on its own line beneath the card, in the colour the rail's status lines use, and it is a quantity, so a quantity of nothing is not drawn. **It is summed from the same list the Clients screen draws its money boxes from**, never written beside it, because a second copy of a figure a screen computes is a second definition (L107). Rejected: taking it off the chrome entirely, keeping it as a fifth count, and recounting it as the clients whose money could not be placed automatically. Rejected in the second round: the card's own label and figure colours, and the figure alone at the card's brightness. `scripts/check-design-sidebar-card.sh` renders every design file that draws an app window and refuses a rail that disagrees with the others, which is what ovation#188 was filed for: the fifth line lived in two of the four files for three days with every check green, because what disagreed was markup and the shell check compares stylesheets.
46c. **The rail's held money figure stays on every screen, including one that already names a single client's held money** (Dan, 2026-09-10, ovation#191). 46b settled the line on the invoice LIST, where no per client figure exists. On the invoice screen the rail's all client total lands a few inches from `Held money applied` and `stays held on the client`, which are one client's, so one composed page carries two figures both labelled about held money and meaning different things, and that composition had never been looked at. Four whole options were rendered in one switcher and Dan kept the screen as it is. **Rejected: dropping the rail's line wherever the page already carries a per client figure**, whose cost was drawn rather than described, because 14h gives a paid invoice no held figure at all and neither has a client holding nothing, so the rail would change shape as an invoice moves through its life. **Rejected: wording it for scope**, as `Held, all clients`. **Rejected: taking it off the chrome on every screen**, which 46b turned down once on its own grounds and which this round put beside the alternative that motivates it. The consequence for code is that no screen may suppress this line for what is on the page under it, which is what `scripts/check-design-sidebar-card.sh` already asserts by requiring the four rails to agree, and it is now a requirement rather than a property of nobody having tried.
46d. **Held money waiting on a decision is a GROUP at the top of the invoice list and a `To place` line in the card** (Dan, 2026-09-10, ovation#190). 14j settles what Ovation does when a client with held money has more than one invoice open: it applies the money to neither and offers `Use it here` on each. That offer was the only surface the case had, so it could be met only by already being on one of those invoices, and it is not rare. **Measured: 24 of 129 payments across 2019 to 2024 arrived while another of that client's invoices was still open, 18.6%, concentrated in 3 of the 41 clients** (`scripts/measure-invoice-history.py`, and it is an upper bound rather than a count: the export carries no payment amounts, so it cannot say how often money was left over). **The group and the card line are one decision rather than two**, because 46a says acting on a card line takes you to the groups it rolls up, so a count with nowhere to go was never a candidate. Rejected: the list unchanged, and the group with no count in the card. **The line counts INVOICES waiting on a decision**, not clients, which is what keeps 46a's rule that every line in the card counts one unit. **The group MOVES its rows rather than copying them**, and the first ordinary row after it carries a rule and air so the list beneath is not read as the group's contents: the first version did neither, drew two invoices twice under a header saying 2, and `scripts/check-design-draws.sh` now refuses a list that draws one invoice twice. **The card is chrome, so the line is on all four screens that draw it**, which `scripts/check-design-sidebar-card.sh` enforces. Money held against a client with NO open invoice is the other half of ovation#190 and is not settled by this: it has no invoice to group.
46e. **Money held against a client with NO open invoice gets no surface outside the Clients screen** (Dan, 2026-09-10, ovation#190, and it is a decision rather than the absence of one). It sits indefinitely, and the question was whether the screen Dan works from should say any of it has nowhere to go. **No requirement says such money is owed back**, which was checked rather than assumed: ovation#190's own body said 5.14a does and 14a says nothing of the kind. What is recorded is 14e, that a refund names the payment it came out of and held money is netted against refunds, and 13, that cancelling an invoice carrying payments asks what happened to the money. Whether unused money is owed back at all is undecided. Three were rendered: nothing, a second quiet line under the rail's `Money held` figure naming the part with no invoice open, and a `To return` count in the card. **He kept nothing.** Rejected with their reasons, because both are reversible only if the reasons are written down: the split rail line states a fact where the fact already is and asks nothing, which is its case and also why it is easy to look past; the `To return` count claims the money is WORK, which every other line in that card is, and it would have to lead somewhere, which today is the Clients screen. **The cost is stated rather than left implicit: nothing in Ovation ever prompts a return.** The money is visible where the client is, on the Clients screen, in the box that names where it came from (14l), and that is the whole of it. Frequency in front of him when he chose: 6 of 171 invoices across 2015 to 2024 were paid before they were issued, median 11 days early, which is a floor rather than a total since the export carries no payment amounts (14g).

47. **A row is one line.** Client and shoot name together, then shoot date, invoice number or the word draft, amount, and the action. Money is always in tabular figures with aligned decimals, because an amount that can be misread is the one thing this app cannot afford. A selected row is marked by a tint alone and never a left bar, which is both the macOS convention and a practical necessity once the sidebar is dark.
47a. **A row that covers several shoots names the LAST one and counts the rest** (Dan, 2026-09-07). It reads "Tosca, opening night" then "+1 shoot", and the date becomes a span, "2 to 6 Sep 2026". The last shoot is named rather than the first because it is the shoot the invoice is DATED by, so the name and the date agree instead of the row naming one shoot and dating another. **The span appears only where the shoots really are on different days**: a matinee and an evening on one date show that single date, because a span from a day to itself states nothing. The year is printed once, at the end, which is what keeps the widest case inside its column.
47b. **The date column is 152px and the row stays one line, both measured in a browser rather than reasoned about.** A span needs 113px and one crossing a month needs 150, against 75 for a single date, so at the old 104px the row silently WRAPPED to two lines and broke 47. The column must be a fixed width: each row is its own grid, so sizing it to its content sizes it per row, and the dates and amounts stop lining up down the list, which was tried and measured at four different left edges. The 48px it gained comes back off the invoice number column, which reserved 58px for content needing 38, and the amount column, which reserved 104px where six figures need 76. Neither over reservation was visible: it fails no test and breaks nothing, it only stops the neighbour growing. **A shorter date format was measured and rejected on 2026-09-07, and the obvious candidate goes the wrong way.** The column has to fit the WIDEST case, which is a span crossing a month, not a single date. Year first (2026-09-06) needs 180px, THIRTY more than the current form, because a span crossing New Year has to print both years. A two digit year saves 15px and dropping the year saves 37px, but the list holds several tax years at once, including a Cancelled group going back years, and nothing else on the row says which. So the format stands at day, month, full year, and this is recorded so it is not re-proposed on the assumption that year first is more compact.

48. **A control says what it does.** "Mark cleared" and "Mark sent", never "Cleared" or "It was sent", which read as statements rather than commands. Actions are drawn as a word rather than a filled button: Downbeat's `DBPlainButtonStyle` is the existing control for this and is already used in 18 places, so nothing new is invented.

49. **The agreed rendering is committed at `docs/design/invoice-list.html`.** One self contained file, no build step, opens in any browser. It is a rendering rather than the app and its README names the three web idioms in it that must be translated rather than copied. It is committed rather than linked so it outlives whatever hosted it.

50. **The invoice PDF answers to the LETTERHEAD, not to the brand kit** (Dan, 2026-09-07). It is the only Ovation surface a client ever sees, so it obeys Dan Wright Photography's stationery rather than the app's espresso interface language in 43 to 49. The brand kit (v1.0, 2025) specifies Deep Teal Blue `#2E5866` and Warm Cream `#F5E6D3`; measured from a render of `Letterhead.psd`, the actual stationery uses neither, and its values are header band `#9FD4C6`, footer bar `#7FA99E`, banner `#221F20`, ground `#F7F5F2`. Dan chose the letterhead, which makes the brand kit the thing that is out of date. Typography is the kit's, since the letterhead does not contradict it: **Merriweather for the title and the amount, Lato for everything else**, both open licensed. Lato's digits are the same width with and without `tabular-nums`, measured in a browser, so this document needs no monospaced face and must not borrow the invoice list's.

50a. **The document answers one question first: what is owed and by when.** The amount due and its date sit at the top right at 25px before anything else, and the itemisation below is evidence rather than the point. Settled over four page shapes rendered at true US Letter proportion. The reason it beat the alternatives is a measurement rather than a preference: 16 of 19 real bookings are a single one hour shoot, so the invoice that actually gets sent is ONE line whose content stops around 550px down a 1056px page, and a shape that only works when the page is full is a shape for a case that does not occur.

50b. **The total is stated twice, deliberately, and only once loudly.** Announced at 25px at the top, and closed at 10.5px at the foot of the arithmetic. This breaks the design rule that no number appears twice, and it is the right break: an invoice is not a screen, and a reader adding up the column has to arrive at an answer rather than at a sentence pointing elsewhere. The quiet weight is what keeps it from reading as the page saying the same thing again.

50c. **A line carries description, hours, rate and amount, so the client can reconstruct every figure** (Dan, 2026-09-07, choosing full disclosure over three quieter options). The consequence, which was measured and shown to him before he chose it: on an ordinary invoice `$250.00` is printed three times, as the rate, the amount and the subtotal, and on a tax exempt one five times. **That is not a defect to be tidied later.** Restating a figure across a calculation is how a reader checks arithmetic, and the whole point of showing the rate is that the arithmetic can be checked.

50d. **The identity is the real mark alone, and the contact details are at the foot.** The mark is `Watermark/Logo Pdf/DanWright black.pdf`, at least 200px wide per the brand kit's minimum, with nothing beside it. Dan's email and phone sit at the foot beside the payment terms, which is where somebody looks when they have a question about the bill. **The signature that appears on the letterhead is deliberately absent**, so nothing needs lifting out of `Letterhead.psd`. The renderer must use the VECTOR original: the committed design file embeds a PNG only because HTML cannot carry the vector, and a raster mark will be soft in print.

50e. **The only colour on the document is one bar at the foot in `#7FA99E`** (Dan, 2026-09-07, over three more colourful options including the full stationery). It is the single element the letterhead ends on, it sits below everything rather than near any text, and nothing on the page depends on it being reproduced. Two measurements decided the losers rather than taste: that teal is 2.60 to 1 against white, under the 3 to 1 a rule needs, so it may never be used as a rule or behind text, and a full mint band across the head costs real ink on the one Ovation surface that gets printed and photocopied.

50f. **The agreed rendering is committed at `docs/design/invoice-pdf.html`**, with the same discipline as 49: one self contained file, no network at all, the decision record inside it, and the web idioms named so they are translated rather than copied. The real artefact is a PDF at 612 by 792 points and must be laid out in points rather than scaled from the 816 by 1056 CSS pixels the rendering uses.

51. **The agreed rendering of the invoice screen is committed at `docs/design/invoice.html`**, with the same discipline as 49 and 50f: one self contained file, no network, the decision record inside it. It shipped as two files and was merged into one on 2026-09-08, because rounds 1 to 8 built the draft being priced and round 9 built what it becomes after sending, and they always shared a shell, a fixture and every rule. **The rules it runs are committed separately as executable cases** under `docs/design/rules/`, and the file carries its own copy of each verbatim because a design file may reach outside itself never; `scripts/check-design-rules-inline.sh` refuses a copy that has drifted. Note that the referral credit is drawn as its own block above the subtotal, which contradicts 5.8 deliberately and is tracked as ovation#126. **The foot's main action on a draft says `Review`, not `Send`**, which 52a corrects and ovation#169 applied to the committed file on 2026-09-09; `scripts/check-invoice-screen-draws.sh` reads the word off what the foot DREW, so the two cannot drift apart again without the check going red.

51a. **The hours are DERIVED from the shoot's real start and end times and are never typed** (Dan, 2026-09-08). Four ways of typing hours for a shoot whose times were not noted were rendered and his answer removed the question rather than choosing between them: "I plan to create drafts with no end time (although I can put the start time in from the creation). When I go to send the invoice I will always know the end time." He always has both times at the moment it matters, so a second way in would be a second source of truth for one number. These are the REAL times, entered after the shoot, never Downbeat's booking times, which 5.3a says nothing may be priced from.

51b. **The invoice names the ONE thing it is waiting on, and names the thing rather than its class** (Dan, 2026-09-08, from the workflow in 51a). A draft carrying a start time and no end time is the ORDINARY state of every draft, so it says `Needs the end time` and its refused Send says `Waiting on the time the shoot ended.` The order is fixed, because two independent refusals mean the screen tells a different story depending on which is answered first: the times, then the duration they produce, then the tax status. The vocabulary is complete and lives in one place, `docs/design/rules/waiting.js`, which returns the reason together with both sentences that carry it, so what is drawn beside the times and what the Send says cannot disagree about one invoice.

51c. **Above twelve hours a duration prices nothing, and the screen refuses it BY NAME** (Dan, 2026-09-08, which settles the naming 5.3b requires). He chose 12 against 8, 7 and no cap at all, with the measurement in front of him: his longest ever billed is 6.25 hours and his median is 1.625. He took the loosest option that still refuses something, and **the cost he accepted is recorded here rather than discovered later**: at 12 a mistyped meridiem on an evening job, 7:00 PM read as 7:00 AM, comes to 10 hours and is priced rather than caught. The screen says `Longer than a shoot` where the figure would be, and `That is more than 12 hours, so it prices nothing. Check the times.` under the Send. Both times being present is a different fact from no times at all, so asking for the times again would be asking for something already given.

51d. **An invoice's audit history is a pane that PUSHES the invoice across rather than covering it**, opened from a word in the invoice header (Dan, 2026-09-07 for the pane and the push, 2026-09-08 for everything else). It is **300px**, chosen against 220, 272 and 330 with each option's consequence measured: 300 is the first width at which every entry settles to two lines including the one carrying a client's real email address, and past it the invoice gives up width for no gain. The invoice therefore has to work at 556px as well as at 856px, which is checked rather than assumed. Entries name **what the system did**, `Draft created` over `from a Downbeat booking`, kept against the same events said as what happened, said tersely, and said as one sentence a line. The control stays in the header, kept against the foot, an edge pull and the View menu. It slides at 280ms with the same easing as the list to invoice transition, so the app has one motion rather than two.

51e. **A discount that exists is changed on a line beneath it, and the Edit menu ADDS one but never removes one** (Dan, 2026-09-08). The line carries the percent and dollar toggle, the value and Remove, and it sits below the discount row rather than inside it because the controls do not fit the totals row's 208px label column without moving every figure off the one shared right edge, which was measured going wrong twice. It was kept against editing the number where it sits, controls on hover only, and nothing on screen at all. **The menu no longer offers Remove**, because with the line always present when a discount exists that was one action offered twice and the menu copy is the one further from the thing it acts on. The referral credit keeps its menu entry, having no controls of its own anywhere.

51f. **A line is added by a word under the table that appends a row, and the service type is chosen inside that row** (Dan, 2026-09-08). Kept against three alternatives, each drawn and clickable: the type chosen from a list at the word before any row exists, an empty row sitting permanently at the end of the table on every invoice, and nothing on the lines at all with the action in the Edit menu. **The menu was a real candidate**, since 51e's rule sends the rare things there, and the measurement is what ruled it out: 26 of 130 issued invoices carry more than one line (20%) against 5 of 130 carrying a discount, half of those being a second event on one invoice and half a second service on one event (`scripts/measure-invoice-history.py`). The row takes a type and then an amount, because neither Rush turnaround nor Preview images carries a default amount anywhere (4 and 5.4). **The amount field draws no placeholder reading `0.00`**, and an amount that cannot be read leaves the row where it is rather than committing a line worth nothing, both for the reason in 5.1b: a zero total is a legitimate comped invoice, so a figure nobody has given the screen is never drawn as one. **Creating a new service type from inside the invoice, which 4 also requires, is deliberately NOT settled here** and was kept out of all four options, because a second variable in one round is what makes a round unreadable.

51g. **A service type that does not exist yet is created from a panel opened by the type list's last entry** (Dan, 2026-09-08), which settles the rest of 4. Kept against an entry that turns the row's description into a name field and against a list searched rather than read, where a name matching nothing is offered as the thing to create. It is the only option where making a type is a separate act from using it, and so the only one that can ask a second question. **It asks what the type usually charges**, which a service type carries (`defaultUnitAmount`) and none of 5.4's three seeded ones has, and the answer is READ: it prefills the amount on the line the type is used for, so the question is not one nothing consumes. **An amount it cannot read is recorded as NO amount rather than as zero**, because a type that charges nothing and a type with no usual amount are different things and one of them would prefill every line with `0.00` (5.1b). **Nothing asks whether the new type is hourly**: the name is Dan's and the role is not, since exactly one type is the hourly photography line and code has to know which. Two things follow from the panel rather than being chosen in it, and are recorded so they can be reversed: it sits below the invoice's header rather than at the top of the window, where it covered the client's name, the shoot and the start time; and Create looks inert until a name is typed, because a control that does nothing and gives no reason leaves pressing it again as the only diagnosis.

51h. **The due date in the invoice's foot opens the terms an invoice is written on, each showing the date it lands on, with another date at the end** (Dan, 2026-09-08), which settles the per invoice half of 7. Kept against typing the date in the foot and against nothing on the foot at all with the action in the Edit menu. Every term NAMES the date it produces, which is what it was kept for over typing one. **It moves this invoice only**: asked where the per client override lives, Dan settled that a client's standing terms are set on the Clients screen and never on an invoice, against offering the standing change in this list and against offering it after the change, so ovation#98 now owes that control. **It was drawn on 2026-09-10 and 51j is what it is.** **Another date opens the same panel 51g uses**, rather than a second panel to keep looking like the first. **A date that cannot be read is refused with a reason and nothing moves**: `31 Sep` rolls forward to 1 Oct in JavaScript's date arithmetic, so an unguarded reader takes a date nobody typed, and the due date is what every chase and every overdue count is timed from. Both dates on the screen are derived from the shoot's one stamp rather than written in three places, in UTC (L39).

51i. **A rare, consequential action is asked about in the panel this screen already has** (Dan, 2026-09-08), kept against asking in the foot with nothing covered, against a macOS sheet from the window's title bar, and against not asking at all and offering an undo afterwards. It was rendered on dismissing a draft, which 1c moved into this screen, and **it is the treatment combining (1a) and cancelling both inherit**, so the question is settled once rather than three times. **The sentence names the shoot and says what happens in the domain**: dismissing is not a delete, so it says the invoice stays in the list recorded as not billed on that date (1b), never that it will be removed, and it names THIS shoot because a warning that reads the same on every dismissal carries no information. **Afterwards the invoice is drawn as what it now is**, its lines and totals quiet, the foot stating the recorded decision in place of the dated line and the Send, and its history still beside it. **There is no way back from a dismissal yet, and that is deliberate.** A `Bill it after all` was drawn in the Edit menu on the reasoning that a recorded decision nothing can revisit is a dead end, and Dan removed it the same day because it was a control he had never been shown: everything else on this screen was chosen from renderings and one arrived at by argument does not get to sit beside them. So the dead end is real and visible rather than papered over by a guess, and undoing a dismissal is ovation#150. What the invoice LIST does on the way back is ovation#125 rather than part of this.

51j. **A client's standing payment terms are changed on the Clients screen by pressing the VALUE, which opens the same four terms 51h offers** (Dan, 2026-09-10, round D of ovation#98). Kept against four chips always shown, which spends a row on every one of 31 clients for something changed on almost none, and against a read only value with a Change word beside it, which is one more press for the same list. **Unlike 51h's control it names no dates**, because a client's standing terms produce a date only once there is an invoice to date. The two screens' term lists are ONE list in the product, and in the design record they are two copies that cannot load each other, so `scripts/check-design-terms-agree.sh` compares them and the push gate refuses a disagreement: a client offered a term no invoice can produce is two vocabularies for one thing, and each file reads as correct alone.

52. **The agreed rendering of the review and send screen is committed at `docs/design/review-send.html`**,
with the same discipline as 49, 50f and 51: one self contained file, no build step, the decision
record inside it, and the chooser it came from stripped out. Settled with Dan over six rounds on
2026-09-09 (ovation#101). It carries the shell from `docs/design/shell/` verbatim (ovation#120) and
it BEHAVES: Send runs, the seconds count, and three switches change the invoice and whether the send
is refused, because none of those states can be seen from a still.

52a. **Review is a SHEET over the invoice, dropping from under the window's title bar** (Dan,
2026-09-09). The invoice screen stays behind it and there is nothing to navigate back from. Kept
against the document as a full screen with the recipients in a rail, and against a third place in
the invoice screen's own language. **Two consequences are requirements rather than drawing.** The
sheet belongs to the WINDOW, not to the desktop: positioned against the screen it starts above the
window's own top edge and is centred on a box wider than it. And **the invoice screen's foot says
`Review`, not `Send`**, because it opens this sheet and a control that says Send should send, which
corrects 51 and the file committed under it.

52b. **The document is the settled invoice page at 47% of actual size, opening to 81% when pressed**
(Dan, 2026-09-09). 380px of an 816px page, in an 800px sheet, and the approval column gives up the
room. It is SCALED and never restyled, which is 10c's consequence for this screen: a review screen
may not re-typeset the page to fit a column, because then the preview is a different document from
the one that is sent. The way to open it is visible at rest and sits ABOVE the page, since the page
is taller than the sheet's visible area and anything beneath it is below the fold.

52c. **The recipients are stated, and WHY only when it is surprising** (Dan, 2026-09-09). The screen
shows the address and nothing else, because going to whoever booked the shoot is the default and
saying so is saying nothing. Where the address is NOT that person it says so and names who was
passed over. Measured on the 2026-09-05 export, which is what decided it: all 31 clients have a main
address, 30 carry a second that is an exact copy of it and 1 carries an empty one, so **not one
client has a genuine override today**, while 1 of 31 genuinely carries two addresses in one field.
An option that reads well on the override and adds a line to the ordinary invoice charges all 31
sends for a case that does not occur. Where several addresses share one reason, the reason is said
ONCE for the group, or it is the same sentence printed twice.

52d. **On send, the sheet BECOMES the outcome** (Dan, 2026-09-09). The document and the recipients
are replaced by one statement of what happened and one way onward, so there is nothing left to read
and nothing to press by mistake. Kept against replacing only the buttons, against a band with the
buttons greyed in place, and against dismissing the sheet so the outcome lands on the invoice
screen. Requirement 33 is met inside it and was the floor in every option: working, still alive and
failed are three visibly different things, the elapsed seconds are drawn so it can never look
stalled, and there is no spinner.

52e. **A warning is a band across the sheet, under the title** (Dan, 2026-09-09), and the two kinds
are drawn differently because they are different things. **The due date already past is information**
(7): nothing can answer it except changing the date, so it carries no control. **A shared address is
a QUESTION** (38c): it carries `That is right`, it stays answered, and the acknowledgement is
recorded against the ADDRESS so changing the address asks again. Kept against grouping both above
the recipients, against putting each beside its cause, and against holding them until Send is
pressed, which asks after the decision has been made.

52f. **The message is editable in place** (Dan, 2026-09-09), which is the most freedom and the most
ways to go wrong, and it is why 41a is not discharged by this screen existing. Whatever is in the
box is what ships. **Every outbound sentence on this screen still owes its cold read**, rendered, in
the state that produces it: the subject, the composed message, and all three sending sentences. The
wording was held identical across every option in every round precisely so no sentence could be
chosen for how it read, which means none of them has been chosen at all. In the app the editing
surface is the design system's own control, never the platform's default; the committed rendering
uses a styled textarea and says so as a web idiom.

52g. **An EMPTY message refuses the send, and the screen says what it is waiting on** (Dan,
2026-09-09, asked the moment 52f settled). Editing in place is what makes an empty message
reachable, so the freedom and the refusal arrived together: what ships is whatever is in the box,
and a blank note under an attachment is not a thing Ovation sends on Dan's behalf. It follows 51b
rather than inventing a second convention: the reason is stated at its cause, under the message,
the Send is GREYED rather than removed, and the greyed control carries what it is waiting for, so
it is never a dead end with no reason on it (L109). **Both halves move together**, which is a
requirement rather than a detail: the first implementation greyed the Send and left the sentence to
appear only on a later redraw, so the control was disabled with nothing anywhere saying why, and it
was caught by driving the control rather than by reading it. One sentence, held in one place, so
what is drawn beside the message and what the Send is waiting on cannot disagree.

---

## 6. How it works

**One list, sorted by what needs Dan.** Invoices live in a single searchable, filterable list. The order, top to bottom:

1. Draft, shoot is today. Send it today.
2. Overdue. Chase it.
3. Draft, shoot has passed. Should have been sent.
4. Paid by check, not yet cleared. Confirm it cleared.
5. Sent, awaiting payment. Waiting on them.
6. Draft, shoot still ahead. Nothing to do yet.
7. Paid or cleared.
8. Cancelled.

Oldest first within each group. Today's shoots outrank overdue invoices because sending today's invoice is a thing done once and now, while chasing can happen any time this week. This ordering replaces the estimate trick Dan used in QuickBooks to keep far off work out of his view.

**CORRECTED 2026-09-06 (ovation#49): those eight groups are NOT the whole state space, and reading them as though they were is how a record becomes unreachable.** The list is the only way to reach an invoice, so a state matching no group is not merely awkward, it is invisible, and an invisible unbilled shoot stays unbilled. Four states have no group above:

- **A draft with no shoot date.** Groups 1, 3 and 6 are all keyed on the shoot date, and an invoice can be created from scratch with no booking behind it, so a dateless draft matches none of them. ovation#49 settles where it goes: **with group 3**, because "should have been sent" is what needs Dan about it. It is not a ninth group.
- **Could not determine whether it was sent.** ovation#45 treats sent as something only ever observed, never asserted, which makes the sent state a triple rather than a pair. The third member is the one that needs a person, and it must not be collapsed into either of the other two.
- **Part paid.** ovation#48 records payments against an invoice, so an invoice can hold some of its money and not the rest. **Settled 2026-09-06: this is NOT a group.** It is a property of the amount. A part paid invoice sits in whatever group its dates already put it in, Chase it when past its terms and group 5 when not, and the row shows the outstanding balance rather than the invoice total. Giving it a group would split one action across two bands for no gain. **The row must say which figure it is showing** (nearer "960.00 owed of 1,240.00" than a bare number), because the year end export deliberately disagrees with it: under the accrual basis in 24 the export carries the invoice's FULL value in the year it was issued, so the same invoice is two different numbers in two places on purpose, and an unlabelled pair reads as a defect.
- **Refunded.** **Settled 2026-09-06 (Dan): this is NOT a group either, and the earlier draft of this correction was wrong about why.** That draft argued refunded could not sit inside Cancelled because the shoot happened and was invoiced. **That case does not exist in this business.** Dan never refunds delivered work. A refund happens only when a client paid a deposit and then cancelled BEFORE the shoot date, so a refund is always a consequence of a cancellation. Refunded is therefore a cancelled invoice that carried a payment, marked as such inside group 8, and requirement 13 already describes exactly that flow. **THE SHOOT DATE DECIDES THE TAX YEAR FOR EVERYTHING ATTACHED TO THE INVOICE, REFUNDS INCLUDED** (Dan, 2026-09-06: "it's cut and dry. based on the shoot date"). A deposit taken in 2026 against a 2027 shoot is 2027 income, because requirement 7 dates the invoice by the shoot. A 30 December 2026 shoot cancelled on the 28th and refunded on 3 January 2027 is entirely 2026, because the shoot was 2026. **This needs no second principle:** requirement 24 already says payment dates are recorded and exported but never decide a tax year, and a refund is a payment going the other way. It also removes 13's prior year worry from refunds specifically, since a refund only ever precedes its own shoot and so can never remove income from a return already filed.

That leaves **could not determine whether it was sent** as the only one still needing a placement, and the design work of 2026-09-06 gives it its own band ("Say whether it was sent") on the grounds ovation#49 states, that the third member of the sent triple is the one needing a person. Everything here is decided on ovation#49, which also carries the property test that is the real enforcement: every combination of state lands in exactly one group, asserted in both directions, so no state appears in two and none appears in none. **A future revision of this list is answerable to that test rather than to this paragraph**, because a list maintained by hand beside a test is the drift the test exists to catch (L41).

**What one booking is.** Downbeat's booking flow books a whole run in one pass, but it saves one `Booking` row per show, each holding a single date, a single time range and a single venue (`ShowCommitPlan` in `BookingDraftBatch.swift`, committed one plan at a time by `BatchCommitOrchestrator`). So "a booking" throughout this document means one performance, and a three night run reaches Ovation as three of them, drafting three invoices. That is the DEFAULT and not the final answer: the unit Dan actually bills is the engagement, and he combines drafts by hand where several shoots are one job (5.1a). Nothing on this handoff carries relatedness, deliberately.

**The booking handoff.** Downbeat writes one small file per committed booking into a queue. Ovation reads it, creates the invoice, saves it, and only then removes the queue file. The order matters: saving first means a crash loses nothing, while acknowledging first would lose the invoice. Downbeat's seven day sweep is untouched, so timing stops mattering entirely. A re run is marked as such and creates nothing new, only a note on the invoice that already exists.

**Cancellations.** Downbeat has no concept of a cancelled booking and cannot tell Ovation about one, so cancelling is done in Ovation. Because Ovation cannot distinguish a cancelled shoot from an invoice Dan forgot, it surfaces drafts whose shoot has passed without being sent, and never guesses which they are. What it CAN do, once 5.1b is built, is let him say: dismissing such a draft records the decision against the row, so the pile of passed drafts is the ones still genuinely waiting on him rather than a mixture of those and every shoot he was never going to bill.

**Receipts.** Dan photographs a receipt at the register and shares it to mail, or forwards a digital receipt, into a folder in Spark. That folder is a Gmail label underneath. Ovation collects them into an unfiled queue, reads what it can, and Dan completes them at the Mac.

**Client identity.** Ovation matches a booking to a client on Downbeat's stable client identifier. An unknown identifier matching exactly one existing **main address** links automatically. **It matches on the main address and not the override** (37a), for two measured reasons: every client has a main address and one has no override at all, and 30 of the 31 overrides are exact copies, so matching on the override would be matching a duplicate of the field next to it while failing outright on the one client that has none. A single name only match asks once. No match at all creates a new client.

More than one match is its own refusal, never treated as no match (L521). Two clients sharing a name, or two sharing an address, stop the match and say so rather than falling through to the create-new path, **and that is deliberately stricter than 38c, which lets a shared address be dismissed as correct when SENDING**. The two questions are different: a person can be told that one treasurer legitimately pays for two clients, and a matcher handed one address and two candidates still cannot tell which booking belongs to which. Answering the send question must never be read as answering this one (L330 applies to the rule that RAISED the question, not to every rule that mentions the same fact), because that path manufactures a duplicate identity which every later invoice, payment and referral credit then feeds. A roster sync failed exactly this way in project-enrollment-tracker: it refused two names on one row and never refused one name on two rows, which its own matcher had just detected.

Where Downbeat later sends a client detail that differs from Ovation's copy, Ovation shows both and asks, once per differing value, never repeatedly for the same one.

---

## 7. Non goals

1. **Accepting payment in the app.** Dan is paid by check, Zelle, Venmo and PayPal and records payments himself. Building acceptance would add fees, compliance and a payment provider to solve a problem he does not have.
2. **Recurring expenses, mileage tracking, part business splits and foreign currency.** All four considered and declined. Dan does not drive to shoots, keeps business and personal separate, and does not buy in other currencies.
3. **Linking expenses to a shoot or client.** Declined, and the only cost is that per job profitability is not answerable. This item used to decline billing expenses back as well, and named the workaround as typing reimbursable costs onto an invoice as ordinary lines. **Dan confirmed on 2026-09-06 that he does not bill costs back at all**, so there is no workaround to name. A cost he incurs is an expense and never also appears as income. ovation#97 raised that double counting as something to confirm deliberately rather than discover in January, and the answer is that it does not arise. The concept is retired, and this note is the only place it is still named, so that anyone who remembers it can find out where it went.
4. **Reconciling against a bank or card statement.** Declined. See section 4.
5. **Tracking contractor payments for 1099 reporting.** Payments to people file as ordinary expenses under a contract labour category. The obligation remains Dan's.
6. **Sales tax filing support.** The accountant files the periodic returns from the same records.
7. **A second Mac, and any phone version.** Filing a receipt from elsewhere works by emailing it.
8. **Ovation deciding, inferring or proposing which shoots belong on one invoice.** Combining itself is now a requirement, 5.1a, added when Dan reversed this item on 2026-09-06. What stays out is any attempt to work out relatedness on his behalf, whether from consecutive dates, a shared Downbeat booking pass, or the same client appearing twice in a fortnight. Every one of those gets his own example wrong in one direction or the other, and a suggestion that is often wrong is one he learns to dismiss without reading (L36). The default is always separate invoices and the correction is always manual. Automatic grouping remains available later, see 10.6.
9. **Anything about delivering photographs.** Galleries, exports and delivery stay in Downbeat and OmniFocus.

---

## 8. What Downbeat provides

This section described work Downbeat had to do before Ovation could be built. **All of it shipped on 2026-08-27**, as issues 426 to 431 in `danwright32/downbeat` and 3193 in `danwright32/overture`. What follows is the contract Ovation is now built against, not a request. The authoritative version lives in the Downbeat repository at `Downbeat/Downbeat/Integration/OvertureExport/CONTRACT.md`, nested two levels deep rather than at the repository root, which carries a table naming every consumer of these surfaces.

1. **The handoff queue.** Directory `~/Library/Application Support/Ovation/booking-queue/`, one file per booking named `<BOOKING-UUID>.json`, written atomically at commit. A Debug launch writes to `booking-queue.debug` instead, and a test process writes nothing at all.
2. **Each record is self contained.** It carries the booking, the client and the venue as they were at the moment of commit, rather than pointing at the export file. This is deliberate: the export is current state, so a client edited or removed after the commit would change what an unconsumed record resolves to. The frozen values are what makes requirement 6's "show both and ask" possible, since Ovation can compare what was true at booking against what Downbeat says now.
3. **The record carries the shoot's real start and end instants**, so Ovation can price the invoice, and `isRerunOf`, so a re-run adds a note to the invoice that already exists rather than drafting a second one.
4. **The file is named for the booking id**, which makes the write idempotent: a commit retry lands on the same path and replaces it rather than queueing the shoot twice.
5. **Downbeat reports its own failures.** A booking it cannot queue raises a problem naming that shoot, and it deliberately never retracts, because a later booking queueing cleanly says nothing about the one that did not. So Ovation is not the only thing standing between a commit and a missing invoice.
6. **A re-run replaces the predecessor's row rather than deleting it** (downbeat#429, Dan's decision 2026-08-27). Before that fix, re-running an upcoming shoot removed its days from the export and Overture's scout stopped suppressing nights already booked. It also meant a second re-run of the same booking answered "Booking not found". Both are fixed, and it is why `isRerunOf` now reaches a saved row at all.
7. **The export version gate.** Overture now accepts any version at or above a minimum rather than an exact set (overture#3193). Ovation must do the same. Refusing a payload whole because its version is unfamiliar produces empty data that is indistinguishable from the data genuinely being gone (L255).
8. **Ovation's minimum is declared where Downbeat can read it, and a test keeps that declaration honest.** Downbeat's push gate refuses a push whose version has outrun a consumer, and it cannot see Ovation: it names Ovation as a second consumer in its contract table and enforces no floor for it (downbeat#437). So Ovation publishes the minimum it accepts in a committed file, `integration/downbeat-handoff-accepted-versions.json`, and a test in this repository ties that file to the **real decoder** by behaviour rather than by reading the constant, because a declared value nothing enforces is the same drift one step along (overture#3203, L27, L70). **Both land in the same commit as the decoder, never before it.** Declaring a minimum while Ovation has no decoder would turn Downbeat's honest "cannot measure" into a green pass over a hand typed number describing no code at all, which is the failure that gate's three outcome design exists to prevent (L98, L11, L182). The filename is a convention shared with Overture rather than Ovation's to choose alone, raised on overture#3203: `integration/downbeat-<surface>-accepted-versions.json`, `export` for Overture and `handoff` for Ovation. If Overture settles on a different shape, Ovation follows it, because two names put Downbeat back to two readers and lose the point.

---

## 9. Open questions and risks

1. **The QuickBooks subscription cost is unrecorded.** Owner: Dan. Without it the primary measure has no baseline.
2. **The volume of 2026 records is unknown.** Owner: Dan. It decides how the importer is built and how long migration takes.
3. **The accrual basis is confirmed by Dan, not by his accountant, and that is worth one more check.** Settled 2026-08-27, see requirement 24. Recorded here rather than closed because of how it was arrived at: Dan first answered the opposite, changed to accrual once the question was put concretely, and reaffirmed after being told it is a fact about his filed returns rather than a preference. He is entitled to that call and the export is built on it. The two minute confirmation, if he ever wants it, is line F of any past Schedule C, which asks for the accounting method and has a box ticked for Cash or Accrual. If that box says Cash, requirement 24 and everything keyed to it are wrong. **The same conversation carries two more accrual questions the export cannot answer for itself (added 2026-08-28):** whether the invoice date (the shoot date, per 7) or the day the invoice was sent decides its tax year, since a December 30 shoot invoiced on January 3 lands in different years under each; and, formerly, how a refund paid out in a later calendar year than its invoice is reported. **That second one is settled and is no longer for the accountant** (Dan, 2026-09-06): the shoot date decides, so a 30 December shoot refunded on 3 January is wholly the December year, consistent with 24's existing rule that payment dates never decide a tax year. The export keys on the invoice date and says so in its manifest. **The cross year refund case is now assertable and must be asserted**, which it could not be while the answer was unknown. It is not written yet: it belongs with the export work in ovation#61 and #63, as a fixture whose shoot, cancellation and refund straddle 31 December and which must land wholly in the shoot's year. Items 9.4 and 9.5 belong to the same conversation, and 26a means the first milestone cannot close until it has happened.
4. **The asset threshold is proposed, not confirmed.** Owner: Dan, with his accountant. $2,500 is the IRS de minimis safe harbour, but it is an election made on the return and Section 179 may make it moot. Note also that the threshold is per item while Ovation sees a receipt total, so it is right on single item purchases and wrong on bulk ones.
5. **Whether sales tax applies to rush and preview charges is unconfirmed.** Owner: Dan, with his accountant. Ovation currently taxes the whole subtotal, matching invoice 1057.
6. **The QuickBooks export format is unverified.** Owner: Dan. A sample of the real CSVs is needed before the importer is built. **Under the accrual basis in 24 that is three exports, not one (added 2026-08-28):** the invoice list (number, date, client, lines, tax, total); the payments or transactions export, so every imported invoice carries its payment state per 24a; and the invoices still open at the end of 2025, because one issued in 2025 and paid in 2026 is 2025 income and must be imported as a prior year invoice carrying a 2026 payment, never as 2026 income.
7. **The two lists in 5.4 and 5.19 are drafts.** Owner: Dan. Correct them here.
8. **The reader is unmeasured on receipts, and the design it replaced was impossible.** Owner: Dan. See 5.18a: the original two reader agreement design could never have worked, because the on device language model takes no image input, and that was discovered by the planning panel rather than by anyone building it. Measure the corrected single reader design against real receipts, with the pass marks agreed and written down first, before the extraction is trusted.
9. **Whether Spark displays Ovation's sent invoices correctly is unverified.** Owner: Dan. Sending through Gmail should place a copy in the Sent folder, but this has not been observed.
10. **Risk: no deadline and no dry run.** Dan chose no delivery date and chose to prove the app by using it rather than by comparing a full year export against QuickBooks. Together these mean the export is first exercised for real in January, with the accountant waiting, and the subscription keeps renewing meanwhile.
11. **Risk: Downbeat's task templates now contradict reality.** Dan chose to leave them alone for now. Until revisited, every shoot seeds OmniFocus tasks instructing him to create an estimate in a QuickBooks he has cancelled.
12. **Risk: 14 days is a change to how clients are treated.** Current invoices give 30. At 14, clients who have always paid at 30 will show as overdue on day 15 without having changed anything.
13. **Risk: Ovation will be able to modify the mailbox.** Filing receipts needs permission to read and change mail, not only to send. That is wider than invoicing alone requires, and it is the price of automatic receipt intake.
14. **An invoice sent outside Ovation is recovered by reading Gmail, and that match is unproven.** Dan cannot assert Sent by hand, so the derived route in 5.10a is the only thing standing between a Spark send and an invoice stuck as a draft for ever. It matches on the invoice number appearing in a sent message, which has not been tested against a real mailbox. Owner: Dan. If the match proves unreliable, the choice in 5.10a has to be revisited rather than quietly leaving the gap L162 describes.
15. **The invoice PDF design is open.** Dan is not satisfied with the current QuickBooks format. The content is settled; the design is separate work.
16. **The payment block on the PDF is currently truncated** mid word by QuickBooks. Dan chose to keep the "available upon request" wording and fix the truncation rather than print his payment handles.

---

## 10. Later versions

Directional, not committed.

1. **A phone version.** Dan expects to want one. The chosen architecture makes it an iPhone app sharing data through iCloud rather than a rewrite. Reopens after the first filing.
2. **Statement reconciliation.** The only mechanism that could make expense capture provably complete. The reason to revisit it is a year end where the expense total looks wrong and nothing can say why.
3. **Retiring Downbeat's QuickBooks era tasks.** Pending Dan, see 9.11.
4. **Sales tax period reporting.** Worth revisiting if the accountant starts asking for quarterly totals.
5. **Line item extraction from receipts.** If the readers prove good enough, splitting a multi item purchase would make the asset threshold accurate on bulk buys, which it currently is not.
6. **Grouping shoots onto one invoice AUTOMATICALLY.** Combining by hand ships in 5.1a. What is deferred is Ovation working out which shoots belong together, declined in section 7 item 8. The reason to revisit it is Dan finding the manual correction frequent enough to be a chore, which he does not expect (2026-09-06). It would need relatedness stated at booking time in Downbeat and carried on the handoff, because neither consecutive dates nor a single booking pass is proof of one job: his own case is a Wednesday rehearsal billed with a Saturday concert.

---

## 11. Sign off

1. Sections 5, 6 and 7 need Dan's confirmation. Every decision in them is his, recorded from the interview, but recorded by me.
2. Items 9.3, 9.4 and 9.5 need Dan's accountant. 9.3 blocks relying on the first export.
3. Item 9.7 needs Dan to correct the two draft lists in this document.
4. Section 8 needs nothing. The Downbeat and Overture work it once asked for shipped on 2026-08-27, so the second success measure is no longer blocked by another repository.

---

## 12. Lessons audit

This document was checked against `~/.claude/LESSONS.md` on 2026-08-27, before any code was written.

One decision was reversed by it. Dan's original choice that only Ovation could set Sent is the shape L162 describes, and the same defect had already shipped in Overture (`replyHandledAt`, overture#2865), with the same mail client. Requirement 5.10a is the revised version: Sent is still never asserted by hand, but it is derived from the Gmail Sent folder as well as from Ovation's own sends.

Nine requirements exist only because a lesson demanded them, and they are marked in place: 5.16a (L104), 5.19a (L113), 5.29a (L2, L191), 5.31 (L39), 6.32 (L51), 6.35 (L98, L100), 6.36 (L512), 6.42 (L83), and the many-match refusal in the client identity part of section 6 (L521).

Already satisfied by decisions Dan made during the interview, with no change needed: L5 and L12 (the handoff saves the invoice before acknowledging the queue file), L7 (backups and a restore path from day one), L15 (matching keys on Downbeat's stable identifier), L47 (the import records its refused rows rather than skipping them), L74 (due dates anchor to the stored invoice date rather than being recomputed from today), and L1 with L48 (the receipt readers are measured against real receipts before being trusted, see 9.8).
